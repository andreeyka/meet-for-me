# App — модуль `app-ui`, владелец DEV-1 [v1.21, MEE-430]

Здесь живут App-таргет (меню-бар, окна, настройки, мастер прав) и composition root —
единственное место, где порты домена связываются с конкретными реализациями.

Каркас Xcode-проекта (`project.yml` для XcodeGen, корень репозитория) заведён архитектором
в MEE-430: App-таргет собирается (`App/Sources/MeetForMeApp.swift` — menu-bar-иконка без
окон, LSUIElement), но пока ничего не делает. Composition root и сами экраны — предмет
следующей задачи DEV-1; постановка — комментарий архитектора в MEE-430.

Entitlements и Info.plist-ключи прав (микрофон, системный звук, календарь) — в `project.yml`
(секции `entitlements`/`info` таргета `MeetForMe`), не отдельными файлами: подпись сертификатом
и её проверка — на живом Mac, за DEV-1.

Запрещено: обращаться к `Storage`, к движку и к системным адаптерам мимо домена (П7).
