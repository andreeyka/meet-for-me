//  SystemWaitSeam — производственная реализация `WaitSeam` (calendar-hub) поверх `Task.sleep`.
//  MEE-433, решение РП п.2 (перечень MEE-347, раздел «Шов»: «Шов — обязателен для DEV-1 до
//  реализации»). Обслуживает разом таймаут-политику «Поведения» (10/120/30с, К9), политику
//  повторов §5.2 и таймер опроса Р5 — тремя разными применяющими сторонами одной инъекции
//  (`WaitSeam` докстринг, `Packages/Core/Sources/CalendarHub/Seams.swift:32-36`).
//
//  Модуль: app-ui · Владелец: DEV-1 · Слой: composition root
//
//  ОТМЕНА — гарантия `Task.sleep(for:)` самой стандартной библиотекой Swift Concurrency, не
//  собственная логика этого файла: если объемлющая задача уже отменена до вызова, бросает
//  `CancellationError` немедленно, не засыпая; если отмена пришла во время сна — просыпается
//  досрочно с тем же отказом (документированный контракт `Task.sleep`, не наблюдение по
//  опыту). Реализация — единственная строка, делегирующая этот контракт целиком.
//
//  ТЕСТ НА ОТМЕНУ — не XCTest. Тестового таргета у App нет: `project.yml` — файл архитектора
//  (docs/module-map.md §2, «правка разработчиком — interface-request, а не коммит»), заводить
//  тестовый таргет — не предмет этой задачи. Поведение, которое проверял бы такой тест, —
//  контракт `Task.sleep(for:)` самой стандартной библиотеки, а не код, написанный здесь
//  заново: XCTest на нём проверял бы Swift Concurrency, не этот файл. Готов завести
//  interface-request на тестовый таргет App, если он нужен для этого или будущих файлов
//  модуля — сейчас не завожу сам, вне сегодняшней Urgent-задачи.

import CalendarHub

struct SystemWaitSeam: WaitSeam {
    func sleep(for duration: Duration) async throws {
        try await Task.sleep(for: duration)
    }
}
