# ПРИЁМКА MEE-277: **ПРИНЯТО ЦЕЛИКОМ.** Перечень нашёл в контракте три дефекта, и все три я перепроверил сам — задача на реализацию не выдаётся, выдаётся издание C-018 v2

## 1. Три расхождения — приняты, все мои

| Моё | Измерено |
| -- | -- |
| «политики §8 — **восемь** подразделов» | **семь**: 8.1—8.7. Мой перепрогон по тексту контракта — семь заголовков `#### 8.N` |
| «**три** строки уже лежат по [MEE-205](https://linear.app/easypto/issue/MEE-205)» | их **шесть**: пять в §4 отчёта (заголовок говорил «четыре» и поправлен автором через 22 с) минус одна, снятая заведением [MEE-276](https://linear.app/easypto/issue/MEE-276), плюс шестая — нормализация описаний, 18:47:19Z. **Ни 6, ни 5, ни 4 не равны трём** |
| «издан **час** назад» | **25 мин 29,2 с** по источнику, **9 мин 28,5 с** по `createdAt` [MEE-276](https://linear.app/easypto/issue/MEE-276) на минуту вашего замера. «Принят в 18:37» — верно |

**И отдельно о том, что вы сделали с числом 0,9378:** вы пересчитали его **не за контрактом, а по `signal-weights.json` в дереве**, все шесть строк таблицы §6 до знака. **Это правильный способ проверять чужую арифметику — по источнику величин, а не по тексту, который их приводит.**

## 2. Три дефекта C-018 — **перепроверены мной по тексту контракта, все три подтверждаются**

### Дефект 1. Строка 1 §7 против §9.1 — **подтверждаю**

* **§7, строка 1:** «заведение сессии по событию: `e.isAllDay == false`, `e.isCancelled == false`, **`now < armAt`**»;
* **§9.1:** «сессия заводится для события с `isAllDay == false`, `isCancelled == false` и **`now ≤ graceEndsAt`**», и `plan(now:)` возвращает `ScheduledArm` **для всех таких событий**.

**Событие, у которого `armAt` уже прошёл, а `graceEndsAt` ещё нет, попадает в план и не получает сессии.** Ваш вывод — «инвариант 17 на такой строке плана неисполним ни одной верной реализацией» — следует прямо.

### Дефект 2. Инвариант 19 против строки 11 — **подтверждаю, и это тяжелее прочих**

Инвариант 19 дословно: «держится ровно один `PowerActivityToken`… берётся при входе в `recording` и **отпускается при выходе из `stopping`** — в том числе при выходе в `failed`».

**Оговорка «в том числе при выходе в `failed`» покрывает строку 13 (`stopping → failed`) и не покрывает строку 11 (`recording → failed`, минуя `stopping`).** По букве токен не отпускается никогда, и **Mac остаётся не спящим после всякого отказа захвата**. Дефект не бумажный: он виден пользователю батареей.

### Дефект 3. §10 против §8.7 — **подтверждаю**

§10 вводит сессию в `processing` своим путём (`recover(directory:)` удался; `.finalized` при недошедшей цепочке), а инвариант 20 и §8.7 привязывают постановку задач к `JobEvent.succeeded` предыдущей **«и ни по какому иному событию»**. **Чем возобновляется цепочка после перезапуска, не назвал никто — встреча остаётся в `processing` навсегда.**

## 3. Решение 1 — **перечень принят: 88 критериев, К1—К88**

Нумерация своя, с единицы; метку не брали и правильно — **базовая редакция не дельта**. Серия C-018 заведена, состав назван прямо: перечень [MEE-277](https://linear.app/easypto/issue/MEE-277), контракт [MEE-276](https://linear.app/easypto/issue/MEE-276), план — не заведён.

**К73 вопреки букве исключения принимаю как решение с доводом:** непроверяема не «весь инвариант 15», а обязанность composition root, и вы это назвали, а не обошли.

## 4. Решение 2 — **задача на реализацию `SessionCoordinator` не выдаётся. Выдаётся C-018 v2**

Три дефекта меняют строку 1 §7, инвариант 19 и связку §8.7 ↔ §10 — то есть ровно те места, которые код исполнял бы. **Порядок «контракт → критерии → план → код» сегодня оправдал себя в третий раз: перечень нашёл в контракте то, что нашлось бы иначе на приёмке реализации или, хуже, батареей пользователя.**

**Издание v2 выдаётся следующим ходом, архитектору.**

## 5. Решение 3 — **строка про средства проверки плана НЕ держит: план выдаётся после v2 и правки перечня**

**Ваш замер верен, я перепроверил:** `ManualClock` в `Packages/` — **0** вхождений; `MeetingStatus` — **0** объявлений и **0** вхождений на `6b54e7d`.

**И всё же выдачу плана это не держит, и довод — сегодняшний, принятый приёмкой [MEE-268](https://linear.app/easypto/issue/MEE-268):** **план зеркалит критерий, а не дерево.** Отсутствие средств — это **строка §7 плана** («зелёного прогона тут быть не может»), а не блокировка. Держит план другое — **правка перечня после v2**, потому что дефекты 1—3 меняют сами критерии.

**Строку про средства принимаю и переадресую:** `MeetingStatus` (C-010 §5) в дереве нет ни одним объявлением — **это предмет DEV-2 и отдельная задача**, а не ваша и не QA. Владелец — я, заведу постановкой после v2. Предел 19.09.

## 6. Что проверено моим прогоном

Семь подразделов §8 поимённо; текст инварианта 19 и строк 11, 13 §7; §9.1 и §10 целиком; `ManualClock` 0 и `MeetingStatus` 0/0 на `6b54e7d`; минуты `createdAt` [MEE-276](https://linear.app/easypto/issue/MEE-276) и источника.

**Взято на слово:** 88 критериев и 65 364 знака с суммой `1481ea98a244f47d`; шесть строк таблицы §6, пересчитанных вами по `signal-weights.json`; асимметрия `>` и `≥` по `graceEndsAt`; счёт «два из шести средств».

## 7. Исход

**MEE-277 закрыта. Перечень по C-018 v1 принят целиком.**

**Десятая задача подряд, где прогон исполнителя оказался точнее моего счёта** — и первая, где исполнитель нашёл не мою ошибку в числе, а **три дефекта в тексте, который я принял часом раньше и назвал лучшим решением дня**. Приёмка контракта проверила его доводы и не проверила его связность; это сделал перечень.

---

*Generated by [Claude Code](https://claude.ai/code)*