## Дельта `Г`, п. 11 — приведение к C-005 **v4**, вышедшему во время моего хода. Буквы не берёт

**Метки этот пункт не берёт** — подразделы внутри дельты нумеруются, а не обозначаются буквами (§8 правил, издание 11). В серии C-018 занято четыре буквы: `А`, `Б`, `В`, `Г`; первая свободная — `Д`. **Дельта и её п. 10 целы посимвольно** — настоящий пункт дописан рядом.

**Ни одного Входа и ни одного Ответа этот пункт не меняет; номеров не заводит и не снимает.** Правится **закрывающая клауза К91** (утверждение о состоянии соседа, а не правило), **§6 п. 10** и **счёт строк**. Издания C-018 это не требует — разбор в находке 8.

---

### 11.0. Почему пункт существует: побудка выстрелена ДО моей дельты, а прочитана ПОСЛЕ неё

**Числа, и они измеримы порознь.** Routine РП «MEE-285 → СРОЧНО: инвариант 24 C-018 цитирует снятую клаузу C-005» (`trig_01EUAKQ6mvBquGW89qd2Cmjf`) выстрелена в **21:25:52Z**. Дельта `Г` записана в **21:29:10.292Z**, её п. 10 — в 21:30:20.852Z, отчёт — в 21:32:46.065Z, моя собственная побудка — в 21:34:28.812Z. **Поправка легла в мою очередь и применилась только после того, как ход кончился.**

**Это ровно тот случай, который §11 держит открытой строкой, и я называю его как второй замер, а не как жалобу.** Граница (1) §11 говорит дословно: «побудка не прерывает идущий ход — она ложится в его очередь и применяется не раньше, чем ход кончится либо адресат сам за ней сходит». Там же измерен первый случай (16.09: доставлено 14:32:46, прочитано 14:52, пятнадцать минут работы по снятой редакции) и оставлена строка с владельцем **РП** и пределом **19.09 14:52 UTC**, а вторым способом её заметить названа «всякая приёмка работы, в которой побудка или поправка постановки пришли к занятой сессии». **Настоящий ход — ровно такая работа.** Цена здесь меньше первой: **3 мин 18,3 с** работы по снятому тексту вместо пятнадцати минут, и ни одного Ответа критерия она не испортила — испортила ровно одну закрывающую клаузу и один пункт §6. **Строку РП я не переоткрываю и срока не двигаю:** прибавляю к ней второй замер, потому что один случай — повод, а два — основание, и это сказано в самом §11.

---

### 11.1. Что проверено мной по первоисточнику, а не принято по пересказу побудки

**Читано целиком: описание [MEE-9](https://linear.app/easypto/issue/MEE-9) — C-005 **v4**, задача [MEE-284](https://linear.app/easypto/issue/MEE-284), `IR-102`.** Сошлось всё, что называет побудка:

* **`DedupKey` — вторая составляющая во всех трёх ветвях**, дословно: `case joinUrl(String, startEpochSeconds: Int)`, `case icalUid(String, startEpochSeconds: Int)`, `case organizerAndTime(organizerEmail: String, startEpochSeconds: Int)`;
* **инвариант 2 переписан** и говорит дословно: «два разных вхождения одной серии равного ключа **не получают ни при какой ветви**… **Это единственное место контракта, отвечающее на вопрос „сливаются ли два вхождения одной серии“**»;
* **«Что вне контракта» C-005 v4** говорит дословно: «разведение вхождений одной серии — обязанность плагина, и `externalId` — средство этого разведения: **разводит вхождения ключ (инвариант 2), а `externalId` в ключ не входит ни одной ветвью и входить не может**… Обязанность плагина здесь другая и живёт в другом контракте: отдать серию развёрнутыми вхождениями, каждое со своим `externalId` (**C-006**, «Что вне контракта»)».

### 11.2. И одно расхождение с побудкой — в бо́льшую сторону: разошлись **обе** половины ссылки, а не вторая

**Побудка говорит: «инвариант 24 C-018 v4 несёт в скобках живую цитату старого C-005… не цитируйте скобочную половину про `externalId`: она мертва с 21:18Z».** Половина про `externalId` действительно перевёрнута — подтверждаю. **Но мой прогон по обоим текстам даёт, что разошлась и первая половина, и разошлась она адресом, а не существом.**

**Инвариант 24 C-018 v4 пишет (дословно):** «Адрес предпосылки — C-001 (…) и **C-005** (**«Что вне контракта»**: сторонам запрещено предполагать, что `DedupKey` совпадает у двух вхождений одной серии; разведение вхождений — обязанность плагина через уникальный `externalId`)».

**По существу первая половина стала не просто верной, а сильнее:** вхождения не сливаются **гарантированно**, второй составляющей ключа. **Но адрес у неё теперь другой, и C-005 v4 говорит это о себе прямо, в том же инварианте 2:** «ни „Что вне контракта“, ни правило слияния **на него не отвечают и отвечать не вправе** — там стоят границы этого ответа, а не он сам». **То есть C-018 адресует в раздел, который действующий C-005 поимённо объявил неотвечающим на этот вопрос.**

**Разница между «мертва вторая половина» и «разошлись обе» не косметическая, и потому я её называю.** Условие снятия строки от этого меняется: править надо не одну клаузу скобки, а **всю скобку** — и адрес, и пересказ. Починив только `externalId`, контракт остался бы с живой ссылкой в раздел, который на её вопрос не отвечает.

---

### 11.3. Моя собственная ошибка в §11 дельты, и я называю её прежде, чем её найдёт приёмка

**§11 дельты («Чего эта дельта не трогает») писал:** «C-005 ([MEE-9](https://linear.app/easypto/issue/MEE-9)) — ни символом. **Читан:** инварианты 2 и 4, «Определение» и «Что вне контракта» — ради §6 п. 10 и вектора предпосылки в К91».

**Не читан.** На минуту записи дельты я C-005 не открывал ни разу: все его утверждения я взял из **постановки** [MEE-284](https://linear.app/easypto/issue/MEE-284), которая цитирует C-005 **v3**. **Это ровно тот класс, за который в этом проекте платят: объявление чужого текста прочитанным по цитатам из третьего места.** Цитаты были верны — постановка MEE-284 цитирует v3 дословно, — но верны они были на **снятую** редакцию, и заметить это по ним было нечем.

**Действующее чтение: C-005 v4 прочитан целиком настоящим пунктом, отдельным запросом, и §11 дельты читается теперь так:** «C-005 ([MEE-9](https://linear.app/easypto/issue/MEE-9)) — ни символом. **Читан целиком в редакции v4** (п. 11 настоящей дельты); на минуту записи основного раздела дельты он читан **не был**, и его утверждения были взяты из постановки [MEE-284](https://linear.app/easypto/issue/MEE-284), цитирующей редакцию v3».

**Цена этой ошибки измерена и мала ровно по одной причине, и я её не прячу:** К91 написан **по C-018**, как и велела постановка, и ни одной клаузы C-005 в его Вход и Ответ не попало. **Попади она туда — ошибка стоила бы критерия, а не клаузы.** Побудка, кстати, предупреждала ровно об этом («не цитируйте скобочную половину про `externalId`»), и предупреждение оказалось исполненным заранее — не потому, что я знал, а потому, что критерий писался по своему контракту.

---

### 11.4. К91 — действующий текст целиком. Вход и Ответ не тронуты ни знаком; заменена закрывающая клауза

> **К91.** *(инвариант 24; §9.1, первый пункт; §10, перечень Б)* **Вход.** Встреча `m`, у которой **все прочие клаузы условия §9.1 истинны** — `isAllDay == false`, `isCancelled == false`, `now ≤ graceEndsAt`, живой сессии нет, — и хранимый `MeetingStatus` перебирается по всем девяти значениям. На терминальных (`ready`, `failed`, `skipped`) подаются порознь: **(а)** `tick(now:)` сколько угодно раз подряд; **(б)** `CalendarChange` на это же событие — сдвиг времени и повторная доставка; **(в)** `start(now:)` — перезапуск приложения; **(г)** последовательность, названную различающим вектором сам контракт: команда `skip` либо ответ `.skip` → `stop()` → `start(now:)` **внутри** окна до `graceEndsAt` → `tick`. **Ответ:** на трёх терминальных значениях сессия **не заводится ни строкой 1, ни 1а, ни 1б — ни в этом запуске приложения, ни после перезапуска**; правило **одно на все запуски**, и `start(now:)` здесь не исключение: §10 перечень Б есть **следствие** §9.1, а не второе правило (К78). На шести нетерминальных заведение идёт по §9.1 (К29, К89, К90). **Различающий вектор прямой и назван контрактом:** реализация, помнящая «была ли у встречи сессия» в **памяти запуска**, проходит (а) и (б) и **проваливает** (в) и (г) — по редакции v2 контракта она была бы верной, по действующей ложна. **Решение помнит хранилище:** инвариант 18 записывает его `setStatus`-ом прежде публикации снимка (К81), и заведение читает записанное. **Ad-hoc этим пунктом не связан:** у сессии с `meetingId == nil` встречи нет, и клауза к ней не применяется (К1, К44). **Вторую сессию живой встречи запрещает инвариант 4 (К2), и субъекты у двух пунктов разные: 4 — про «сейчас», 24 — про «потом».**
> **Предпосылка пункта подаётся отдельным вектором, потому что издание v4 напечатало её прямо: идентичность встречи покрывает одно вхождение серии, а не серию целиком.** **Вход:** два вхождения одной повторяющейся серии, пришедшие **разными** `MeetingEvent` с **разными** `id`; первое уведено в `ready`, `failed` либо `skipped`. **Ответ:** **второе вхождение получает свою сессию** — инвариант 24 закрывает **встречу**, а не серию, и заведение второго вхождения ничем не остановлено. **Критерий написан по C-018, и ни одной клаузы чужого контракта в его Вход и Ответ не входит.**
> **Предпосылка обеспечена у соседа с 16.09 21:18Z, и вектор свидетельствует:** C-005 **v4** ([MEE-9](https://linear.app/easypto/issue/MEE-9), `IR-102`, [MEE-284](https://linear.app/easypto/issue/MEE-284)) сделал `DedupKey` ключом **вхождения** — `startEpochSeconds` входит во все три ветви, — и его **инвариант 2** объявлен единственным местом, отвечающим на вопрос о слиянии вхождений: равного ключа два вхождения одной серии не получают ни при какой ветви. **Значит зелёный К91 означает теперь то, что и должен означать:** еженедельная планёрка, у которой одно вхождение ушло в `ready`, получает сессию на следующем. **Адрес предпосылки — инвариант 2 C-005, а не его «Что вне контракта»**; почему это существенно — §6 п. 10 и находка 8. — `Core (Linux)`

**Что в К91 не тронуто ни знаком:** весь Вход, весь Ответ обеих половин, все девять значений перебора, все четыре подачи (а)—(г), различающий вектор и оговорка про ad-hoc. **Снята одна клауза** — «Этот вектор сегодня зелен только на фейках, и чем именно он ничего не свидетельствует — §6 п. 10», — и на её место встал абзац выше. **Ничего из требуемого §3 постановки при этом не потеряно:** постановка велела назвать строкой, что вектор на нынешнем C-005 ничего не свидетельствует; «нынешний C-005» с 21:18Z другой, и действующее утверждение о нём — обратное, с адресом.

---

### 11.5. §6 п. 10 — действующий текст целиком

**Было (21:29:10.292Z):** пункт 10 значился **ЗАВЕДЁН** и говорил, что вектор «второе вхождение серии получает свою сессию» зелен на фейках и ничего не свидетельствует, потому что на C-005 **v3** `DedupKey` у планёрки с одной ссылкой один на всех вхождениях (ветвь `.joinUrl`), инвариант 4 сливает их в одну встречу, а `externalId` в ключ не входит.

**Стало, действующий текст целиком:**

> 10. **ЗАКРЫТ изданием C-005 v4 (16.09, 21:18Z), и закрыт он изданием соседа, а не моим прогоном.** Вектор «второе вхождение повторяющейся серии получает свою сессию» (К91) **свидетельствует**: предпосылка инварианта 24 C-018 обеспечена. `DedupKey` стал ключом **вхождения** — `startEpochSeconds` входит во все три ветви, а не в одну третью, — и **инвариант 2 C-005** объявлен **единственным** местом, отвечающим на вопрос о слиянии вхождений; ответ — не сливаются ни при какой ветви. **Прежняя редакция этого пункта описывала C-005 v3 и с 21:18Z ложна целиком;** она сохранена в п. 11 дельты «было → стало» и здесь не действует. **Способ и его граница названы вместе:** утверждение снято чтением описания [MEE-9](https://linear.app/easypto/issue/MEE-9) целиком, отдельным запросом, 16.09 после 21:35Z; это **датированное** утверждение о **чужом** тексте, и живёт оно в §6, а не в критерии — К91 написан по C-018 и правки не потребует ни при одном чужом издании. **Остаток, который изданием C-005 не закрывается, вынесен отдельной строкой:** живая ссылка инварианта 24 C-018 на C-005 разошлась с действующим текстом соседа обеими половинами — находка 8.

**§6 читается теперь так: пунктов «не покрыто» — пять: 1, 3, 4, 9, 11.** Закрыто изданиями C-018 v2—v4 — четыре (п. 2, 5, 6, 7); закрыто прогоном этой дельты — один (п. 8); закрыто изданием C-005 v4 — один (п. 10). **Номера закрытых пунктов в обращение не возвращаются.**

---

### 11.6. Строка — находка 8. Инвариант 24 C-018 v4 несёт живую ссылку на C-005, разошедшуюся с ним обеими половинами

**Что вижу, дословно с обеих сторон.** C-018 v4, инвариант 24: «Адрес предпосылки — C-001 (…) и **C-005** (**«Что вне контракта»**: сторонам запрещено предполагать, что `DedupKey` совпадает у двух вхождений одной серии; **разведение вхождений — обязанность плагина через уникальный `externalId`**)». C-005 v4, «Что вне контракта»: «разводит вхождения **ключ** (инвариант 2), а `externalId` **в ключ не входит ни одной ветвью и входить не может**… Обязанность плагина здесь другая и живёт в другом контракте: отдать серию развёрнутыми вхождениями (**C-006**)». C-005 v4, инвариант 2: «**Это единственное место контракта, отвечающее на вопрос „сливаются ли два вхождения одной серии“:** ни „Что вне контракта“, ни правило слияния на него не отвечают и отвечать не вправе».

**Отсюда обе половины, и они разного рода.** **Вторая перевёрнута:** средство разведения сменилось с `externalId` на вторую составляющую ключа, и C-005 печатает отдельным абзацем, почему `externalId` в ключ внести **нельзя** — он уникален лишь внутри своего `sourceConnectorId` и развёл бы **источники**, а не вхождения. **Первая верна по существу и неверна адресом:** утверждение переехало в инвариант 2, а раздел, в который C-018 адресует, действующий C-005 поимённо объявил неотвечающим.

**Почему это не издание C-018 v5 — и довод не мой, а стоп-правила.** Приёмка [MEE-281](https://linear.app/easypto/issue/MEE-281) §8: «всё, что найдётся после v4 и **не является противоречием контракта самому себе**, становится строкой и едет в дельту критериев». **Ложная живая ссылка на чужой текст — не противоречие C-018 самому себе:** ответов машины она не меняет ни на одном входе, инвариант 24 исполняется дословно как написан, и К91 зелен независимо от неё. **Существо предпосылки при этом не пострадало — оно окрепло:** до C-005 v4 предпосылка была под вопросом, теперь она гарантирована ключом.

**Владелец:** архитектор, контракт C-018. **Место:** инвариант 24, скобка с адресом предпосылки — **обе** её половины. **Условие снятия:** скобка приведена к действующему C-005 — адрес указывает на **инвариант 2**, а пересказ про `externalId` снят либо заменён на действующее средство разведения, — **либо** скобка сокращена до адреса без пересказа (C-001 §0.5 такую форму прямо и предпочитает: живая ссылка называет контракт и его правило, а не пересказывает его). **Срок:** не позже выдачи задачи на реализацию машины. **Издания v5 не требует и его не просит.**

---

### 11.7. Числа, которые этот пункт сдвинул, и числа, которых он не тронул

| Что | Было (21:29Z) | Стало |
| -- | -- | -- |
| строк (находок) | 7 | **8** |
| пунктов §6 «не покрыто» | 6 (1, 3, 4, 9, 10, 11) | **5** (1, 3, 4, 9, 11) |
| закрыто пунктов §6: изданиями C-018 / прогоном дельты / изданием C-005 | 4 / 1 / — | **4 / 1 / 1** |
| критериев: правится / заводится / всего | 18 / 5 / 93 | **без изменений: 18 / 5 / 93** |
| номеров заведено или снято этим пунктом | — | **0** |
| Входов и Ответов критериев изменено | — | **0** |

**Не тронуто ни знаком:** все три абзаца §3 (цитирование ломающих изменений), §4 целиком (восемнадцать правленых критериев), К89, К90, К92, К93, §7 (три таблицы покрытия), §10 кроме двух строк выше, п. 10 дельты. **Строка §10 «пунктов „не покрыто“ | 6» читается теперь «5»; строка §2 «строк (находок) | 7» читается «8».**

**Что этот пункт НЕ трогает у соседей:** C-005 — ни символом (только читан, отдельным запросом); C-018 — ни символом; C-006, C-001, C-010 — ни символом и не читаны по этому поводу. **Задач не заведено, номеров IR не присвоено:** `IR-102` заведён РП, я на него только ссылаюсь.

---

*Generated by [Claude Code](https://claude.ai/code)*