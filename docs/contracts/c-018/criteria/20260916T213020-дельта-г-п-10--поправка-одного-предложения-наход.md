## Дельта `Г`, п. 10 — поправка одного предложения находки 1. Внесена **до** отчёта, своим прогоном. Буквы не берёт

**Метки этот пункт не берёт** — подразделы внутри дельты нумеруются, а не обозначаются буквами (§8 правил, издание 11). В серии C-018 занято четыре буквы: `А`, `Б`, `В`, `Г`; первая свободная — `Д`.

**Дельта цела посимвольно** — поправка дописана рядом, а не вместо: в основном разделе не тронуто ни знака.

### Что было неверно — и это дефект набора, а не утверждения

**Находка 1 называет механизм расхождения числа и приводит три буллета контракта как образцы.** Образцы набраны инлайн-кодом **внутри** инлайн-кода: обратная кавычка открывает код-спан, а следующая — закрывает его на слове `estimate`, и предложение рендерится не тем, чем оно написано. **Утверждение при этом верно, и ни одно число находки 1 не сдвигается:** 24 в v4, 21 по ущербному шаблону, 20 → 18 в v3, 19 → 17 в v2, 15 → 13 в v1. **Правится то, как предложение читается, а не то, что оно говорит.**

**Называю это потому, что находка 1 — про число, которое не воспроизвелось из-за шаблона счёта.** Печатать разбор такого класса предложением, которое само сломано разметкой, — ровно тот случай, когда форма спорит с содержанием.

### 10.1. Находка 1, §9 дельты — второй абзац, предложение первое и второе

**Было (21:29:10.292Z), дословно:**

> **Механизм — один на все четыре текста, и он воспроизводится в одну строку.** Три буллета раздела открываются **инлайн-кодом**, а не полужирным: `* `estimate` **служит условием…**`, `* `MeetingStatus` **принадлежит этому контракту**…` и `* `recover` **оставляет** `RecordingStatus`…`. **Счёт шаблоном «строка, начинающаяся с звёздочки и двух звёздочек» их не видит.**

**Стало, действующий текст целиком:**

> **Механизм — один на все четыре текста, и он воспроизводится в одну строку.** Три буллета раздела «Что вне контракта» открываются **инлайн-кодом**, а не полужирным, — это буллеты про `estimate`, про `MeetingStatus` и про `recover` (третий заведён изданием v4). Каждый из них начинается звёздочкой, пробелом и обратной кавычкой; остальные двадцать один — звёздочкой, пробелом и двумя звёздочками. **Счёт шаблоном «буллет, открывающийся полужирным» этих трёх не видит.**

**Остальной текст находки 1 не тронут ни знаком**, и проверка по четырём изданиям (`24 − 3 = 21`; `20 − 2 = 18`; `19 − 2 = 17`; `15 − 2 = 13`) стоит там, где стояла.

### Числа дельты, которых эта поправка не сдвинула

Критериев — **93**, К1—К93 подряд, пропусков **0** (проверено прогоном по записанному телу); правится — **18**, заводится — **5**, снято номеров — **0**; оговорок снято — **5**; пунктов §6 — **6**; строк — **7**; метка — `Г`; область прогона метки — **одиннадцать текстов серии, из них десять измерены суммой 255 000 знаков**. **Ни одно из них поправкой не затронуто: правится набор одного предложения, а не счёт.**

**Издания эта правка не требует и переизданием дельты не является** (§8): она не меняет ни одного критерия, ни одного числа и ни одного адреса; полное «было → стало» напечатано здесь, в проверяемом месте.

---

*Generated by [Claude Code](https://claude.ai/code)*