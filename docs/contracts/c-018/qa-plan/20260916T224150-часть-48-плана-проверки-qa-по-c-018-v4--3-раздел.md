**Часть 4/8 плана проверки QA по C-018 v4 — §3, раздел З: критерии К47—К66.** Головная часть 1/8 с §0—§2 и оглавлением лежит первым комментарием этой ветки.

### З. Одна группа — одна запись; политики §8 — К47—К66

| К | Вход | Ожидаемый ответ | Чем проверяется | Различающий вектор — какую неверную реализацию красит |
| -- | -- | -- | -- | -- |
| **К47** (инв. 13) | сессия `A` в `recording` с `target?.appKey == k`; вторая сессия `B`, к которой отнесена цель с тем же `appKey`; **и то же при `A` в состоянии `stopping`** | **не существует** двух сессий в `recording` или `stopping` с равным `target?.appKey`; строки 6, 8 и 16 у `B` **не срабатывают**, пока `A` держит цель. Проверяется в **обоих** состояниях `A` | `Д` | реализацию, проверяющую занятость **только по `recording`**: она зелена при `A` в `recording` и красна при `A` в `stopping` — а `stopping` держится до прихода манифеста, то есть ровно то окно, в которое второй созвон и начинается. **Одно слово условия, один вектор** |
| **К48** (§7.1) | команда `startRecording`, упёршаяся в занятую цель — **и** для сессии с событием, **и** для ad-hoc (`meetingId: nil`) | бросает `SessionError.alreadyRecording(sessionId:)`, и `sessionId` в ошибке — **той сессии, которая занимает цель**, а не той, что просит. Не `nothingToRecord`, не молчаливый отказ | `Д`; тип ошибки и поле `sessionId` сверяются порознь | реализацию, кладущую в ошибку `sessionId` **просящего** (его она держит в руках, а занимающего надо искать): она зелена на типе ошибки и красна на поле — и цена ошибки прямая, потому что фасад по этому полю показывает человеку, **какая встреча** уже пишется |
| **К49** (§8.1) | событие `e`, `armLeadSeconds` со значением, **отличным** от умолчания 120 (подаются 600 и 0) | `armAt == e.start − armLeadSeconds` при любом значении поля; с этого момента сессия наблюдает вход §2, считает оценку и **вправе записать** — строка 6 срабатывает до `e.start` (К34). Число «2 мин» из `architecture.md` §3.2 в текстах машины константой **не встречается** (К19) | `Д` + `Й` (путевая, условие `Т`) | реализацию **с зашитыми 120**: она зелена на умолчаниях и красна при `armLeadSeconds = 600` — сессия взведётся на восемь минут позже назначенного, и ранний вход, ради которого настройку и растянули, не сработает |
| **К50** (§8.2 `.auto`) | `recordingPolicy == .auto`; появляется звучащая цель — **в `armed` и в `awaitingSignal`** порознь | строки 6 и 8 срабатывают **сразу**; спрос `.recordThisMeeting` **не поднимается ни разу**; `askAt == nil` в `ScheduledArm` | `Д` + `Е` (счёт `promptRaised` за прогон — ноль) | реализацию, поднимающую спрос при `.auto` **и для сессии с событием** — ту, которая прочла §8.6 («ad-hoc спрашивает и при `.auto`») шире её субъекта: она красна здесь и зелена в К60. **Два пункта разводят два случая одного слова «спрашивает», и порознь ни один из них этого не делает** |
| **К51** (§8.2 `.ask`) | `recordingPolicy == .ask`; `tick` в момент `askAt` — то есть сессия существовала **раньше** `askAt`; далее порознь: ответ `.record`; ответ `.skip`; ответа нет до `graceEndsAt` | в момент `askAt` поднимается спрос `.recordThisMeeting` с `sessionId` этой сессии; строки 6 и 8 срабатывают **только после** `.record`; `.skip` уводит в `skipped` (строки 4, 9); без ответа сессия живёт до `graceEndsAt` и уходит в `skipped`. **До ответа наличие звучащей цели записи не начинает** | `Д` + `Е` | реализацию, начинающую запись при `.ask` **по появлению цели**, не дожидаясь ответа («цель есть — человек в созвоне, спросим потом»): она зелена на ветви `.record` и красна на ветвях `.skip` и «ответа нет» — запись началась там, где человек её не разрешал. **Вход «сессия заведена позже `askAt`» здесь не подаётся намеренно — это другой объект, и он заведён К93** |
| **К52** (§8.2 `.manual`) | `recordingPolicy == .manual`; звучащая цель есть; `tick` проходит `askAt`, `e.start` и `graceEndsAt` | спрос **не поднимается ни разу**, `askAt == nil`; строки 6 и 8 **не срабатывают никогда**; запись начинается **только** командой `startRecording`, и она срабатывает (К58). Без команды сессия уходит в `skipped` по `graceEndsAt` | `Д` + `Е` | реализацию, трактующую `.manual` как `.ask` («всё равно спросим — человек разрешит»): она красна счётом спросов и красна началом записи после ответа. **Различение существенно: `.manual` — это отказ от автоматики, а не отсрочка её** |
| **К53** (§8.2, спрос — выход) | сессия с поднятым спросом в `armed` и в `awaitingSignal`; проходят `e.start` и `graceEndsAt` | `SessionSnapshot.state` равен `armed` либо `awaitingSignal` — десятого значения нет; сессия **продолжает считать сроки** при висящем спросе и уходит в `skipped` по `graceEndsAt`; снимок **не говорит**, висит ли спрос, — это отдельный ответ `prompts()` и отдельное событие `promptRaised` | `Д` + `Е` | реализацию, **останавливающую счёт сроков при висящем спросе** («ждём человека — срок не идёт»): она красна уходом в `skipped` по `graceEndsAt`, и это единственный вектор, который её ловит. **Клауза «десятого значения нет» зелена по построению и названа в §5:** `MeetingStatus` объявлен чужим контрактом девятью значениями, и десятого не собралось бы ни у одной реализации |
| **К54** (§8.2, прав не читает) | тексты машины; и поведенчески — `recordingPolicy == .ask` при **любом** состоянии прав, подаваемом фейком прав | **ноль** вхождений `PermissionSnapshot`, `PermissionsPort`, `permissionsReady`, `.notifications` в текстах машины; поведение **не меняется** от того, выдано ли право на уведомления или на системный звук: спрос поднимается одинаково | `Й` (сплошная) + `Д` | реализацию, **не поднимающую спрос без права на уведомления** («спрашивать нечем — уведомление не дойдёт»): она красна поведенческой половиной. **Обязательность `.notifications` при `.ask` — правило C-016, и критерия на неё здесь нет:** вектор проверяет, что машина этого правила **не знает**, а не что оно исполнено |
| **К55** (§8.3) | сессия в `awaitingSignal`; в момент `graceEndsAt` — три набора: **(а)** сигналов нет вовсе; **(б)** есть актуальный `clientRunning` с группой **и** `microphoneInUse`, но **ни одного** `clientAudioOutput`; **(в)** есть актуальный `clientAudioOutput` с группой | в (а) и (б) сессия уходит в `skipped`; в (в) — не уходит | `Д` | реализацию по **отвергнутому контрактом чтению** — «сигнала нет» как «пуст набор актуальных сигналов»: она зелена на (а) и (в) и красна **ровно на (б)**. **Вход (б) держит набор непустым намеренно, и без него пункт не отличает взятое чтение от отвергнутого ничем** — это цена, которую контракт назвал вслух, и единственное место плана, где она проверяется |
| **К56** (§8.4, правило 1) | сессия в `recording`; `tick` с `now > e.end` при **актуальной** звучащей цели, сколько угодно раз подряд | переход `recording → stopping` **не наступает**; `e.end` не стоит ни в одном условии строки 10. Запись продолжается, пока есть цель | `Д` | реализацию, останавливающую запись **по концу окна события**: она красна здесь и в К38. **Разница пунктов названа: К38 проверяет, что условие строки 10 полно, К56 — что оно не шире**, и зелёный одного без другого ничего не значит |
| **К57** (§8.4, правило 2 — момент отсчёта) | последняя актуальная цель с `observedAt == t`; цель больше не публикуется; машина замечает это на `tick` в момент `t + signalTtlSeconds + δ` при **`δ > 0`**. Отдельно: цель появилась снова **до** истечения срока | переход `recording → stopping` наступает в момент **`t + signalTtlSeconds + silenceStopSeconds`** — отсчёт от момента, в который цель **выпала из слияния**, а **не** от момента, когда машина это заметила. При возврате цели переход не наступает и отсчёт начинается заново | `Д` | реализацию, считающую срок **от замеченного**: она опаздывает ровно на `δ`, и вектор задаёт `δ > 0` именно затем, чтобы эта разница существовала. **При `δ = 0` обе реализации отвечают одинаково — то есть вектор, поданный без задержки замечания, зелен по построению**, и это сказано здесь, чтобы `δ` не сочли деталью подачи |
| **К58** (§8.4, правило 3; команда сильнее) | `stopRecording(recordingId:now:)` в `recording`, когда §8.4 останавливать не собирался (цель актуальна, окно не кончилось); и `startRecording` при `recordingPolicy == .manual` и при висящем спросе | `stopRecording` переводит в `stopping` **немедленно, в момент вызова**, а не на следующем `tick`; `startRecording` начинает запись при `.manual` и при висящем спросе. Команды §3.1 исполняются в момент вызова — единственное исключение из «вход до `tick` состояния не меняет» | `Д` — ответ читается снимком **до** следующего `tick`; `Е` — событие в потоке приходит до `tick` | реализацию, **складывающую команды до следующего `tick`** (один путь исполнения на все входы — устройство простое и соблазнительное): она зелена на всяком векторе плана, где после команды идёт `tick`, и красна ровно здесь, потому что снимок читается **между** командой и `tick`. **Это довод за то, что пункт читает снимок в этом месте, а не в конце** |
| **К59** (§8.4, `systemSilent`) | `CaptureEvent.systemSilent` в состоянии `recording` — при актуальной цели и при пропавшей, при любых значениях полей события, сколько угодно раз подряд | остановка **не наступает ни при каких значениях**: `systemSilent` условием строки 10 не является. Пару «тишина в тапе при открытом выводе» машина складывает и по ней запись не останавливает | `Д` + `З` (перебор по значениям полей события) | реализацию, останавливающую запись **по тишине в тапе** («звука нет — созвон кончился»): она красна прямо, и цена её названа контрактом — выключилась бы запись созвона, где говорит только пользователь. **Куда подсказка идёт дальше — не закрыто ничем** (§6 п. 4 перечня), и критерия на её показ человеку здесь нет |
| **К60** (§8.6, ad-hoc) | звучащая цель, **не отнесённая ни к одной** сессии (§5.4, правило 4), при каждой из трёх политик | при `.auto` и `.ask` поднимается спрос `.recordThisMeeting` с `sessionId` **новой** сессии `origin == .adHoc` в состоянии `awaitingSignal` и **`expiresAt == nil`**; при **`.manual` спрос не поднимается вовсе**. Ответ `.record` уводит в `recording` (строка 16), `.skip` — в `skipped` | `Д` + `Е` | три порознь: реализацию, **не спрашивающую при `.auto`** (прочла `.auto` как «записывай всё»), — красна, и это читается наоборот, потому вектор и подаёт все три политики; реализацию, **спрашивающую при `.manual`**, — красна; реализацию, ставящую `expiresAt` **сроком**, — красна на поле, и следом на К61, где спрос обязан сняться причиной, а не временем |
| **К61** (§8.6, спрос снимается сам) | ad-hoc-спрос поднят; **цель перестала быть актуальной**; ответа не было | спрос снимается **сам**: в `changes()` приходит `promptWithdrawn(promptId:)`, `prompts()` его больше не отдаёт, и сессия уходит в `skipped` | `Е` + `Д` | реализацию, читающую `expiresAt == nil` как «держится вечно»: она зелена на всех векторах, где ответ приходит, и красна здесь — спрос про кончившийся созвон висит в `prompts()` до конца работы приложения. **`expiresAt == nil` значит «держится, пока держится причина», и этот вектор — единственное место плана, где два чтения различаются** |
| **К62** (§8.7; инв. 20, первая половина) | вход в `processing` **строкой 12**; далее `JobEvent.succeeded` каждой задачи по очереди | порядок цепочки — **`transcode(recordingId)` → `transcribe(recordingId, profileId, language: nil)` → `diarize(recordingId, profileId)` → `attribute(transcriptId, meetingId)`**; первая ставится при входе строкой 12; каждая **следующая** — **по `succeeded` предыдущей и ни по какому иному событию**: ни по `tick`, ни по `blocked`, ни по `failed(willRetry: true)`, ни по чужому `succeeded` | `Ж` (условие `Н`: журнал `submit` — порядок и счёт) | реализацию, ставящую **всю цепочку сразу** («очередь сама разберётся с порядком»): она красна журналом — четыре `submit` на входе в `processing` вместо одной. **Граница пункта названа и не унесена шире: «ни по какому иному событию» сказано о задаче, которая цепочку продолжает.** Входов, при которых машина ставит задачу **не** по `succeeded`, ровно два, оба цепочку **заводят**, и проверяются они К92; К62 их не запрещает и запрещать не вправе |
| **К63** (§8.7, поля задач) | цепочка, поставленная от входа в `processing`; `AppSettings.defaultProfileId` задан; `JobEvent.succeeded` задачи `diarize`; и та же цепочка у **ad-hoc**-сессии | `profileId` во всех задачах равен `AppSettings.defaultProfileId`; задачи собираются `JobSubmission.standard(_:runAfter:)` с `runAfter == now`; `language` у `transcribe` — `nil`; `transcriptId` для `attribute` машина берёт **из хранилища** (заголовок транскрипта записи, C-010), а не из `JobEvent`; `jobId` разрешается в нагрузку методом `job(id:)`, поле `Job.payload`; `meetingId` у `attribute` равен `meetingId` сессии и **равен `nil` у ad-hoc** | `Ж` (условия `Н` и `С`: журнал `submit`, `job(id:)` и `jobs(status:)`) | две порознь: реализацию, берущую `transcriptId` **из события**, — красна тем, что `JobEvent.succeeded` несёт только `jobId` и тип, и брать оттуда нечего; и реализацию, подставляющую `meetingId` **сессии** у ad-hoc, — красна на ad-hoc-ветви. Вторая парна К1: **одна ошибка устройства, два пункта с разных сторон** |
| **К64** (§8.7, `blocked`) | `JobEvent.blocked` любой задачи цепочки, сколько угодно раз; и `JobEvent.failed(willRetry: true)` | сессия **остаётся в `processing`** на обоих; следующая задача **не** ставится; `setStatus` не зовётся; блокировка провалом не считается | `Д` + `Ж` (условие `Н`: ноль `submit` и ноль `setStatus`) | две порознь: реализацию, трактующую `blocked` как провал, — красна состоянием (и она же красна в К43); и реализацию, ставящую **следующую** задачу по `blocked` («предыдущая встала — двинем дальше»), — красна журналом, а состоянием неотличима от верной |
| **К65** (§8.7, `summarize`) | цепочка, доведённая до `succeeded` задачи `attribute`; и тексты машины | задача `summarize` **не ставится ни разу**; в текстах машины ноль мест, ставящих её. Сессия уходит в `ready` строкой 14 | `Ж` (условие `Н`) + `Й` (путевая, условие `Т`) | реализацию, дописавшую `summarize` пятым звеном («конвейер же кончается резюме»): она красна журналом — пятый `submit` для задачи, у которой обработчик в Срезе 1 не зарегистрирован, то есть задача повиснет в очереди навсегда |
| **К66** (§8.7, повторная обработка) | сессия в `failed`; далее `retryJob`/`retranscribe` заводят задачи, и приходит `JobEvent.succeeded` задачи `attribute` **этой** записи | сессия **остаётся в `failed`**; строка 14 из терминального состояния не срабатывает (инв. 3); `setStatus` в `ready` машина **не** зовёт | `Д` + `Ж` (условие `Н`: ноль `setStatus`) | реализацию, у которой строка 14 **не проверяет терминальности**: она красна здесь и в К5 — **одно место, два пункта, и оба нужны**: К5 подаёт вход как перебор по терминальным состояниям, К66 — как штатный продуктовый сценарий. **Цена названа автором контракта и здесь не чинится:** встречу, обработку которой человек починил сам, не приведёт к фактическому состоянию **никто**; владелец — C-016, место — §6 п. 9 перечня |