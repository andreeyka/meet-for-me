**Часть 5/8 плана проверки QA по C-018 v4 — §3, разделы И, К, Л: критерии К67—К80 и К91—К93.** Головная часть 1/8 с §0—§2 и оглавлением лежит первым комментарием этой ветки.

### И. Питание и сон — К67—К69

| К | Вход | Ожидаемый ответ | Чем проверяется | Различающий вектор — какую неверную реализацию красит |
| -- | -- | -- | -- | -- |
| **К67** (инв. 19) | сессия, проведённая от входа в `recording` до выхода по **каждому** достижимому пути: строка 10 → 12 (`stopping → processing`), строка 10 → 13 (`stopping → failed`), **строка 11 (`recording → failed`, минуя `stopping`)** | на всё время `recording` и `stopping` держится **ровно один** `PowerActivityToken` с `reason == .recording`; берётся при входе в `recording`; **после выхода из множества `{recording, stopping}` не держится ни один — по каждому из трёх путей**. Взятых и отпущенных поровну, остаток нулевой | `Ж` — **условия `Н` и `О`**. Наблюдаемо сегодня: `liveActivities` у `FakePowerPort` даёт и «ровно один с `reason == .recording`» в любой момент, и «остаток нулевой» после выхода. **Не наблюдаемо сегодня: «взятых и отпущенных поровну»** — счётчиков выдач и снятий у фейка нет, `nextId` приватен, а повторный `end()` списка не меняет, так что двойное снятие и неснятие одного из двух выглядят одинаково. **Половина ответа условна по `О`** | реализацию, **отпускающую токен только «при выходе из `stopping`»** — то есть исполнившую букву редакции v1: она зелена на путях 10→12 и 10→13 и красна на строке 11, где `liveActivities` остаётся непустым после входа в `failed`. **Цена её названа контрактом: Mac остаётся не спящим после всякого отказа захвата.** *Вторая половина инварианта не проверяется ничем автоматическим:* что система при взятом токене **действительно не уснула** — `М`, единственный ручной сценарий этого плана (§6 п. 3 перечня) |
| **К68** (§9.3, `didWake`) | `PowerEvent.didWake` после сна, за время которого прошли `armAt`, `e.start`, `graceEndsAt` и срок §8.4 **нескольких** сессий | машина зовёт `reschedule(now:)` и `tick(now:)`; **сроки, пройденные во сне, исполняются первым `tick` после пробуждения** — это инвариант 14 дословно (К11), отдельного правила для сна нет, и второго механизма в текстах машины нет | `Д` + `Ж` (условие `Н`: `reschedule` и `tick` вызваны) + `Й` (путевая, условие `Т`) | реализацию **со вторым механизмом «догнать сроки сна»** — циклом по пропущенным моментам: она зелена на конечном состоянии одной сессии и красна двумя способами — текстом (второй механизм есть) и потоком, где срок исполняется дважды, а `setStatus` зовётся дважды на одну смену |
| **К69** (§9.3, `willSleep`) | `PowerEvent.willSleep` в состояниях `recording` и `stopping` порознь | запись **не останавливается**, состояние не меняется, токен не отпускается, `stop()` захвата не зовётся | `Д` + `Ж` (условие `Н`) | реализацию, останавливающую запись по `willSleep` («машина засыпает — писать некому»): она красна прямо. Разрывы шкалы и маркеры сна — предмет C-004 и C-002, и критерия на них здесь нет |

### К. `Scheduler` — К70—К76

| К | Вход | Ожидаемый ответ | Чем проверяется | Различающий вектор — какую неверную реализацию красит |
| -- | -- | -- | -- | -- |
| **К70** (§3.2, `ScheduledArm`) | событие `e` и `AppSettings` со значениями **всех пяти** полей, отличными от умолчаний; `recordingPolicy` подаётся всеми тремя значениями | `armAt == e.start − armLeadSeconds`; `askAt == e.start − askLeadSeconds` **при `.ask`** и **`nil` при всех прочих**; `startsAt == MeetingEvent.start`; `endsAt == MeetingEvent.end`; `graceEndsAt == e.start + missingSignalGraceSeconds`. **Пять полей — пять отдельных проверок** | `Д` | реализацию, заполняющую `askAt` **всегда** («поле есть — посчитаем, а спрашивать решит координатор»): она зелена при `.ask` и красна при `.auto` и `.manual`, и красна **только полем**, потому что спроса она всё равно не поднимает (К50, К52). Без сверки поля эта ошибка не ловится ничем и всплывает у следующего читателя `ScheduledArm` |
| **К71** (§3.2, `plan(now:)`) | хранилище с событиями, у которых `graceEndsAt` прошёл, равен `now` и впереди; **и с равными `start`, то есть равными `armAt`, и разными `meetingId`** | `plan(now:)` возвращает `ScheduledArm` для **всех** событий с `now ≤ graceEndsAt` и **ни одного** с `now > graceEndsAt`; порядок — по возрастанию `armAt`, при равенстве — по `meetingId`; вызов **сессий не заводит и состояния ни одной сессии не меняет** | `Д` + `И` (повторный прогон на равных `armAt` — порядок обязан совпасть) + `Ж` (условие `Н`: ноль `setStatus`) | реализацию, отдающую строки **с `now > graceEndsAt`**, — красна прямо; и реализацию **без доопределения по `meetingId`** — красна способом `И` на равных `armAt` нестабильным порядком. **Названная неполнота: клаузу «по возрастанию `armAt`» не краснит ни один вход.** `armAt = start − armLeadSeconds` при **одном глобальном** `armLeadSeconds` — монотонная функция от `start`, поэтому порядок по `armAt` и порядок по `start` совпадают тождественно, и реализация, сортирующая по `start`, зелена по построению (§5, строка 4) |
| **К72** (инв. 16) | один и тот же набор «хранилище × `AppSettings` × `now`», поданный повторно, из параллельных задач и при **переставленном** порядке строк хранилища | `plan(now:)` — **чистая функция**: повторный вызов даёт равный ответ поэлементно; порядок ответа **не зависит** от порядка строк хранилища; состояние ни одной сессии не меняется ни одним вызовом | `И` + `Ж` (условие `Н`) | две порознь: реализацию, отдающую строки **в порядке хранилища**, — красна на переставленном входе, и **только на нём**: на неперестановленном её ответ совпадает с верным; и реализацию, **заводящую сессии внутри `plan`** («раз посчитали — сразу и взведём»), — красна тем, что после `plan` без `tick` появились снимки |
| **К73** (инв. 15) | наборы живых сессий с известными сроками **и** планы с известными `armAt`, в трёх раскладах: ближайший срок у живой сессии при **пустом** плане; ближайший `armAt` у плана при сессиях с более далёкими сроками; и набор, в котором сроков **нет ни одного** | `nextDeadline(now:)` **не больше** ближайшего срока любой живой сессии и **не больше** ближайшего `armAt` из `plan(now:)`; возвращает `nil` **тогда и только тогда**, когда сроков нет ни одного. Проверяется **обеими** половинами «тогда и только тогда», на фейках портов | `Д` | реализацию, считающую `nextDeadline` **только по плану** (`Scheduler` знает план, а сроки сессий живут у координатора — разделение соблазнительное): она зелена на втором раскладе и красна на первом — отдаёт `nil` либо слишком поздний момент при живой сессии, чей `graceEndsAt` вот-вот наступит, и composition root не разбудит машину в срок. **Первый расклад — единственный вектор плана, который эту реализацию ловит** |
| **К74** (инв. 17) | для **всякой** строки `plan(now:)` — без изъятий — прогон `tick` по её `armAt` и по её `askAt`; строки подаются трёх видов по `now` заведения: `now < armAt`; `armAt ≤ now < startsAt`; `startsAt ≤ now ≤ graceEndsAt`. Отдельно — строка, встреча которой **уже ушла** в `skipped`, `failed` либо `ready` строками таблицы | **согласие тотально по строкам плана.** Для всякой строки сессия её встречи существует **не позже первого `tick(now:)`** — заведённая строкой 1, 1а или 1б, либо заведённая раньше и живая, либо терминальная, если уже ушла. **Состояние есть функция от `now` заведения:** `now < armAt` — `scheduled`, и в `armed` не раньше `armAt` и не позже первого `tick` с `now ≥ armAt`; `armAt ≤ now < startsAt` — **сразу `armed`**; `startsAt ≤ now ≤ graceEndsAt` — **сразу `awaitingSignal`**. При `.ask` спрос по тому же правилу относительно `askAt`, а у сессии, заведённой при прошедшем `askAt`, — в момент заведения (К93) | `Д` + `Е` (условие `П`) | реализацию, заводящую **всякую** строку в `scheduled` — редакцию v1 дословно: она зелена на первом виде и красна на втором и третьем, где сессия окажется в `scheduled` вместо `armed`/`awaitingSignal` и потеряет `signalTtlSeconds` на догон. **Названная неполнота: К74 вместе с К71 не решает, отдаёт ли `plan(now:)` строку встречи с терминальным хранимым `MeetingStatus`** — обе реализации проходят оба пункта, а различие наблюдаемо через инвариант 15 (§5, строка 5; §8, строка 4) |
| **К75** (§9.1) | событие с `isAllDay == true`; событие с `isCancelled == true`; событие **без `conference`**, у которого ссылка на созвон живёт в `bodyText` либо в `location`; обычное событие | **события на весь день сессий не порождают** ни одной; отменённые не порождают; **отсутствие `conference` заведению не мешает** — сессия заводится, потому что запись всё равно требует звучащей цели. Вход ad-hoc остаётся доступен и для событий на весь день | `Д` | реализацию, **требующую `conference` для заведения** («нет ссылки — нет созвона»): она зелена на двух запрещающих векторах и красна на третьем — и цена её прямая, потому что ссылку в `bodyText` кладёт половина корпоративных приглашений. Прибавившиеся клаузы условия §9.1 здесь **не дублируются намеренно** — они покрыты К29, К89, К90, К91, и второй адрес одного правила был бы вторым определением |
| **К76** (§9.2) | по вектору на каждое правило: **(i)** событие сдвинулось вперёд так, что `now < armAt` нового окна; **(ii)** сдвинулось назад, новый `graceEndsAt` **ещё не прошёл**; **(iii)** сдвинулось назад, новый `graceEndsAt` **уже прошёл**; **(iv)** отменено (`isCancelled == true`) и отдельно удалено — в состояниях `recording`, `stopping` и `processing` | (i) → `scheduled` (строка 5); (ii) сроки пересчитываются **на месте**, состояние не меняется; (iii) → `skipped` (строка 3); (iv) **не трогает сессию ни в `recording`, ни в `stopping`, ни в `processing`** — ни изменение окна, ни отмена, ни удаление; остановку решает §8.4. `isCancelled` не есть удаление, и встреча остаётся в списке | `Д` + `З` (перебор по случаям `CalendarChange` × три состояния) | реализацию, уводящую сессию в `skipped` **по отмене события в `recording`** («встречу отменили — писать нечего»): она зелена на (i)—(iii) и красна на (iv) — обрывает запись идущего созвона, который в эту минуту звучит. **Находка QA про клаузу (iii) живёт в чужой серии C-009 ([MEE-75](https://linear.app/easypto/issue/MEE-75)) и к этому К76 отношения не имеет ни малейшего:** ряды не сквозные, и совпадение номера с совпадением буквы клаузы — совпадение, а не адрес |

### Л. Восстановление после перезапуска — К77—К80, К91—К93

| К | Вход | Ожидаемый ответ | Чем проверяется | Различающий вектор — какую неверную реализацию красит |
| -- | -- | -- | -- | -- |
| **К77** (§10, перечень А) | хранилище с записью в **каждом** значении `RecordingStatus` — перебором по самому перечислению, а не по счёту значений. Состав задач записи задаётся через `jobs(status:)`. **Различающий вектор издания v4: два `start(now:)` подряд** на одной и той же записи, начавшей в `.recording` | `.recording`/`.stopping`: `recover(directory:)` (C-004). **Удалось** — машина **сперва приводит `RecordingStatus` к `.finalized`** (`RecordingRepository.save(_:)`) **и только затем** вводит сессию в `processing` и ставит первую недошедшую задачу (К92); **порядок наблюдаем и проверяется последовательностью вызовов**. **Бросил `recoveryFailed`** — статус приводится к `.failed`, сессия входит в `failed`, файлы остаются на диске (К85). `origin` из `manifest.meetingId`: `nil` → `.adHoc`. `.finalized`: сессия входит в `processing` **тогда и только тогда**, когда цепочка не дошла до успешной `attribute` **и ни одна задача не завершилась отказом или отменой**; есть такая — вход в **`failed`**. `.failed`: сессии не заводит. **После первого `start` запись стоит в `.finalized`; второй `recover` на ней не зовёт и из `unfinalized()` её не получает** | `З` (перебор по перечислению — тест обязан упасть при добавлении значения) + `Ж` (условия `Н` и `С`: порядок `save` → вход → `submit`; счёт вызовов `recover`) + `Д` | три порознь: реализацию, **оставляющую строку в `.recording`** (редакция v3 дословно), — красна на втором `start`: `recover` зван второй раз, и запись не финализируется никогда; реализацию, **входящую в `processing` прежде приведения статуса**, — конечным состоянием неотличима от верной и красна **только порядком в журнале**; реализацию, входящую в `processing` при `.finalized` **с провалившейся задачей**, — красна: повторную обработку заводит команда пользователя, а не восстановление |
| **К78** (§10, перечень Б) | хранилище со встречей в **каждом** из девяти значений `MeetingStatus` — перебором по перечислению; у части встреч есть незавершённая запись, у части нет ни одной. **И отдельно: первый `tick` после `start(now:)`** | `scheduled`, `armed`, `awaitingSignal` → сессия заводится заново **по §9.1** — по тому же единственному условию, **включая клаузу о хранимом `MeetingStatus`**, — и все сроки считаются от `now`; поднятый спрос не восстанавливается и при `.ask` поднимается заново (К80, К93). `recording`, `stopping`, `processing` → судьбу определяет **запись** по перечню А (К77); **записи нет ни одной → машина ставит `failed` и называет это в логе**. `ready`, `failed`, `skipped` → сессия не заводится, **и это следствие §9.1, а не второе правило: тот же отказ обязан наблюдаться и на первом `tick` после `start`**. **Порядок чтения значим: сперва записи, потом встречи** | `З` + `Д` + `Ж` (условие `Н`: порядок чтения — сперва `unfinalized()`, потом встречи) | реализацию, **отказывающую терминальной встрече только в момент `start`** — редакцию v2 дословно: она зелена на девяти значениях в момент `start` и красна на первом же `tick`, который заводит сессию заново. **Это единственный вектор плана, который отличает «следствие §9.1» от «второго правила»**, и без клаузы про первый `tick` пункт зелен по построению на обеих реализациях |
| **К79** (§10, два перечня) | пара, в которой перечни расходятся: встреча в `MeetingStatus.ready` при записи в `RecordingStatus.recording` | исход берётся из **А**: сессия входит в `processing` и доводит цепочку; `setStatus` приводит встречу к фактическому состоянию — и это происходит **прежде** публикации снимка (инв. 18, К81). Перечень Б для встреч в `recording`, `stopping`, `processing` своего исхода не решает | `Д` + `Ж` (условие `Н`) | реализацию, решающую эту пару **по перечню Б** («встреча терминальна — сессии нет»): она красна прямо, и цена её в том, что запись, лежащая на диске недоделанной, не финализируется никогда. **Пара выбрана намеренно из тех, где перечни дают разные ответы; на согласных парах пункт зелен по построению у обеих реализаций** |
| **К80** (§10; ответ на спрос не хранится) | **три ветви порознь, и различает их то, чем кончился спрос до перезапуска.** **(а)** спрос поднят и **не отвечен**; `stop()`, затем `start(now:)`; `.ask`; статус нетерминален и `now ≤ graceEndsAt`. **(б)** спрос отвечен **`.skip`** — встреча в `skipped`. **(в)** спрос отвечен **`.record`** — встреча в `recording` либо дальше | **(а)** сессия заводится заново по §9.1, **поднятый спрос не восстанавливается**, ответ нигде не хранится, и спрос поднимается **заново** — в момент заведения, если `askAt` прошёл (К93). **(б)** сессия **не заводится вовсе** и спрос не поднимается: заведение читает терминальный `MeetingStatus` (К91). **(в)** судьбу определяет запись по перечню А (К77), спрос не поднимается. Во всех трёх сессия перезапуск не переживает; `sessionId` новый, и К7 этим не нарушен | `Д` + `Е` (счёт `promptRaised` по каждой ветви порознь) | две порознь: реализацию, **хранящую ответ на спрос** (в колонке или в файле — «человек уже ответил, зачем спрашивать дважды»), — красна на (а): спрос не поднялся заново; и реализацию, **заводящую сессию на (б)**, — красна, и это та же реализация, что красит К91 (в) и К32. **Три ветви разведены потому, что на отвеченном спросе он не спрашивается ни на одной из двух, и вход «поднят и отвечен» требовал бы взаимно исключающих ответов** |
| **К91** (инв. 24) | встреча `m`, у которой **все прочие** клаузы §9.1 истинны — `isAllDay == false`, `isCancelled == false`, `now ≤ graceEndsAt`, живой сессии нет, — и хранимый `MeetingStatus` перебирается по **всем девяти** значениям. На терминальных порознь: **(а)** `tick(now:)` сколько угодно раз; **(б)** `CalendarChange` на это же событие — сдвиг времени и повторная доставка; **(в)** `start(now:)` — перезапуск приложения; **(г)** названная контрактом последовательность: команда `skip` либо ответ `.skip` → `stop()` → `start(now:)` **внутри** окна до `graceEndsAt` → `tick`. **Плюс вектор предпосылки:** два вхождения одной серии **разными** `MeetingEvent` с **разными** `id`, первое уведено в `ready`, `failed` либо `skipped` | на трёх терминальных значениях сессия **не заводится ни строкой 1, ни 1а, ни 1б — ни в этом запуске, ни после перезапуска**; правило одно на все запуски, и `start(now:)` не исключение. На шести нетерминальных заведение идёт по §9.1. **Вектор предпосылки: второе вхождение получает свою сессию** — инвариант 24 закрывает **встречу**, а не серию. Ad-hoc этим пунктом не связан (К1, К44). Вторую сессию **живой** встречи запрещает инвариант 4 (К2): 4 — про «сейчас», 24 — про «потом» | `З` (перебор по девяти значениям) + `Д` + `Ж` (условие `Н`: заведение читает хранилище) | реализацию, **помнящую «была ли у встречи сессия» в памяти запуска**: она проходит (а) и (б) и **проваливает (в) и (г)**. **По редакции v2 она была верной, по действующей ложна** — вектор красит прежнее правило. **Граница зелёного названа в §7 и строкой в §8, а не оговоркой здесь:** зелёный вектор предпосылки **не доказывает**, что повторяющаяся встреча будет записана больше одного раза, потому что C-005 v4 закрыл одну половину предпосылки из двух и печатает отдельной строкой, что вторую — плагин, отдающий серию одним событием, — **не ловит и отличить нечем ни одним полем** |
| **К92** (инв. 20, вторая половина) | **перебор по всем местам, из которых машина могла бы поставить задачу.** **(i)** вход в `processing` **строкой 12** — первая задача цепочки. **(ii)** восстановительный вход по §10 — по вектору на каждый случай: запись `.recording`/`.stopping` после удавшегося `recover`; запись `.finalized`, у которой цепочка не дошла до `attribute` и **ни одна** задача не завершилась отказом или отменой. Состав задач через `jobs(status:)` задаётся так, чтобы недошедшей была **поочерёдно каждая из четырёх**. **(iii) третье место — перебором:** `tick(now:)` в `processing`; `JobEvent.blocked`; `JobEvent.failed(willRetry: true)`; `PowerEvent.didWake`; **повторный `start(now:)` на той же записи** | (i) и (ii) ставят задачу; в (ii) — **ровно одну**: первую по порядку цепочки, у которой нет ни успешно завершённой задачи, ни ждущей, ни идущей. **Третьего входа нет:** ни один вектор (iii) задачи не ставит, включая повторный `start(now:)`, который на `.finalized`-записи `recover` не зовёт вовсе (К77) | `Ж` (условия `Н` и `С`) + `З` (перебор по четырём недошедшим и по пяти местам) | **два различающих вектора, и они противоположны:** реализацию, поставившую при восстановительном входе цепочку **целиком**, — красна на (ii) четырьмя `submit` вместо одной; и реализацию, **не поставившую ничего**, — красна на (ii) отсутствием. Обе зелены на (i). **Третья: реализацию, дочитывающую хранилище по `didWake`, — красна на (iii).** По базовой редакции перечня критерия на это не было вовсе (§6 п. 5, закрыт) |
| **К93** (§8.2 `.ask` при позднем заведении) | `.ask`; событие `e` становится известно машине **после** своего `askAt` — тремя путями порознь: `CalendarChange`; `tick(now:)` по хранилищу; `start(now:)` (§10, перечень Б). Моменты заведения оба: `askAt < now < e.start` (строка 1а) и `e.start ≤ now ≤ graceEndsAt` (строка 1б). Отрицательные: та же подача при `.auto` и при `.manual`; и заведение при `now < askAt` | спрос `.recordThisMeeting` с `sessionId` этой сессии поднимается **в момент заведения** — срок **не «догоняется»** задним числом и **не пропускается**; `SessionPrompt.raisedAt` равен **моменту заведения**, а не `askAt`. При `.auto` и `.manual` спрос не поднимается ни разу, `askAt == nil`. При заведении **до** `askAt` спрос поднимается в момент `askAt` по общему правилу (К51) | `Д` + `Е` | две порознь: реализацию, **пропускающую спрос**, если `askAt` прошёл («срок истёк — спрашивать поздно»), — красна отсутствием спроса, и человек не узнает о встрече, которая идёт; и реализацию, **«догоняющую» срок** с `raisedAt == askAt`, — красна **только полем `raisedAt`**, а счётом спросов неотличима от верной. **Второй вектор — единственное место плана, где сверяется `raisedAt`** |