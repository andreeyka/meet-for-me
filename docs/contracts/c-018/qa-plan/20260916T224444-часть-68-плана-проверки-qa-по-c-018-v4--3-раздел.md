**Часть 6/8 плана проверки QA по C-018 v4 — §3, разделы М, Н, О: критерии К81—К88.** Головная часть 1/8 с §0—§2 и оглавлением лежит первым комментарием этой ветки.

### М. Порядок публикации и поток изменений — К81, К82

| К | Вход | Ожидаемый ответ | Чем проверяется | Различающий вектор — какую неверную реализацию красит |
| -- | -- | -- | -- | -- |
| **К81** (инв. 18) | **четыре вида.** **(i)** всякая смена состояния сессии с `meetingId != nil` — по вектору на каждую строку §7, у которой источник и приёмник различаются; потребитель, который по событию `changes()` **сразу читает хранилище**. **(ii)** команда §3.1, уводящая сессию в терминальное состояние, — `skip(meetingId:now:)` и `answer(promptId:_:now:)` с ответом `.skip`: наблюдается момент **возврата управления** относительно момента возврата `setStatus`. **(iii)** падение между переходом и записью: фейк `MeetingRepository` **не возвращает** управления из `setStatus` либо бросает. **(iv)** сессия с `meetingId == nil` | **(i)** `setStatus(_:meetingId:)` вызван **прежде**, чем снимок опубликован; потребитель, прочитавший хранилище по событию, читает **новое** значение; порядок проверяется наблюдением последовательности вызовов, а не конечным состоянием. **(ii)** команда **возвращает управление не раньше**, чем её исход записан: последовательность «`setStatus` вернул → команда вернула» обязана наблюдаться в этом порядке на **обеих** командах. **(iii)** управление не вернулось — значит человек подтверждения не получил, и потеря решения на этом векторе **законна**: критерий проверяет **границу** окна, а не отсутствие окна. **(iv)** `setStatus` не зовётся ни разу | `Ж` (условие `Н`: журнал с порядком) + `Е` + **условие `Р`** для (iii): наблюдать «управление не вернулось» нечем, кроме **предела ожидания в тесте**. **Это единственный пункт плана, стоящий на ожидании, а не на заданном моменте** | реализацию, **публикующую снимок и возвращающую управление из `skip` до возврата `setStatus`** — то есть ту, что пишет в хранилище «в фоне, чтобы не держать вызывающего»: она проходит (i) целиком, потому что порядок «вызван прежде публикации» у неё соблюдён, и **проваливает (ii)**, потому что команда вернулась раньше записи. **Цена её названа контрактом: решение человека, потерянное падением, неотличимо от неотданного.** Отдельно: реализацию, зовущую `setStatus` у ad-hoc-сессии с подставленным `meetingId`, — красна на (iv), и это третий пункт, ловящий ту же ошибку устройства (К1, К63) |
| **К82** (инв. 21) | **два и более** вызова `changes()`; подписка в момент, когда уже живут нетерминальные сессии и подняты спросы; изменения, наступившие **до** подписки и **после** неё | **каждый вызов возвращает свой поток**; при подписке поток отдаёт **снимок всех нетерминальных сессий и всех поднятых спросов**, и только за ним — изменения после подписки; **предыстории сверх снимка поток не отдаёт**; всякое изменение приходит **целиком каждому** живому потоку и между подписчиками не делится | `Е` (условие `П`) — два подписчика, полный набор требуется у **каждого** | **различающий вектор назван самим контрактом:** реализацию, **вернувшую на два вызова один сохранённый поток**, — она законна по сигнатуре `AsyncStream`, потому что `AsyncStream` доставляет элемент ровно одному ожидающему, и **молча разделит** изменения между потребителями. Одному подписчику она отвечает как верная; красит её только подача **двух** подписчиков с требованием полного набора у каждого. Отдельно: реализацию, отдающую при подписке **всю предысторию**, — красна изменениями, наступившими до подписки |

### Н. Границы, сборка, поведение — К83—К86

| К | Вход | Ожидаемый ответ | Чем проверяется | Различающий вектор — какую неверную реализацию красит |
| -- | -- | -- | -- | -- |
| **К83** (инв. 23) | сборка `Packages/Core` работой `Core (Linux)`; тексты всех типов контракта | ни один тип C-018 **не импортирует системных фреймворков** (`AppKit`, `CoreAudio`, `EventKit`, `IOKit`, `AVFoundation`, `UserNotifications` и прочих) и не требует их для сборки: `SessionOrigin`, `SessionSnapshot`, `SessionPromptKind`, `SessionPrompt`, `SessionPromptAnswer`, `SessionChange`, `SessionError`, `ScheduledArm`, `SessionCoordinator`, `Scheduler` собираются на Linux. Публичная поверхность `domain-core` разрешённым списком **не описывается**, и своего списка этот план не заводит (исход (б) IR-066, карта модулей) | `Л` — сборка; ответом служит сам её исход | реализацию, импортировавшую `EventKit` в тип контракта — например, чтобы принять `EKEvent` в `ScheduledArm`: она красна сборкой на Linux и **зелена на всяком поведенческом векторе плана**, потому что работа `Core + Mac` её собирает. **Именно это делает способ `Л` здесь незаменимым: поведение у неё верное** |
| **К84** («Данные на границе») | тексты машины и наблюдение **всех** значений, уходящих наружу, **на всех путях, включая `start(now:)` по §10** | на границе **ни одного** файлового пути и ни одного `URL`, **кроме `CaptureRequest.directory`**, который машина получает от хранилища (C-010 §1) и передаёт в захват **не разбирая** — не склеивая, не проверяя существования, не создавая каталога; ни одного типа системного фреймворка. **Наружу уходят восемь значимых значений:** `SessionSnapshot`, `SessionPrompt`, `SessionChange`, `SessionError`; `CaptureRequest` (C-004); `JobSubmission` (C-013); `MeetingStatus` через `setStatus`; **и `RecordingRecord` через `RecordingRepository.save(_:)`** — приведение `RecordingStatus` восстановительным входом (§10, перечень А). Владельца времени жизни нет ни у одного, кроме `PowerActivityToken` (К67) | `Ж` (условие `Н`: сверка аргумента `CaptureRequest.directory` с тем, что отдало хранилище) + `Й` (путевая, условие `Т`) | реализацию, **склеивающую путь** — `directory` плюс имя файла записи («захвату же нужен файл, а не каталог»): она красна сверкой аргумента с полученным значением, и никаким чтением снимка не ловится. **Критерий написан по §10, а не по списку «Наружу» раздела «Данные на границе», и это существенно:** список отстал от §10 на одно значение и `RecordingRecord` не называет (находка 6 дельты). Пункт, написанный по списку, красил бы **верную** реализацию на первом же `start(now:)` с оборванной записью |
| **К85** («Поведение») | **(i)** `stop()` при идущей записи; **(ii)** `ProcessMonitorError.permissionRequired` и `CalendarError` на входах; **(iii)** любой путь машины при любых входах | **(i)** `stop()` **снимает подписки и идущую запись не останавливает** — `stop()` захвата не зовётся, токен не отпускается, состояние `recording` сохраняется; оборванная запись подбирается §10 через `recover`. **(ii)** отказы входов **сессий не роняют**: сигналов просто нет, и сессия уходит в `skipped` по §8.3 своим сроком — **не** в `failed`. **(iii)** машина **не удаляет ничего** — ни файлов записи, ни строк хранилища, ни задач очереди: ноль вызовов удаления на всех путях | `Д` + `Ж` (условие `Н`: ноль вызовов `stop()` захвата, `end()` токена и любых удаляющих методов) | две порознь: реализацию, **останавливающую запись в `stop()`** («приложение закрывается — закроем и запись аккуратно»), — красна на (i), и цена её в том, что §10 нечего будет восстанавливать, а `recover` не вызовется никогда; и реализацию, уводящую сессию в **`failed`** по `CalendarError`, — красна на (ii): человек увидит провал встречи вместо пропуска, а причину ему сообщает мастер прав, а не состояние встречи |
| **К86** («Поведение», порядок фаз в один `tick`) | один `tick(now:)`, в который к моменту вызова накопились сигналы, события захвата, очереди, календаря и питания, **и одновременно наступил срок §7** — вход построен так, что пришедший сигнал **меняет** исход срока | порядок **назван и наблюдаем**: сперва применяются пришедшие события, **затем** пересчитывается оценка и звучащая цель, **затем** проверяются сроки в порядке строк §7 — то есть срок решается уже **на новой** цели, а не на прежней. Вход, пришедший **между** двумя `tick`, до `tick` состояния не меняет — **кроме команд §3.1** (К58) | `Д` + `Е` | реализацию, **проверяющую сроки прежде применения событий** (естественный порядок «сначала время, потом почта»): она красна на этом входе прямо — сессия уходит в `skipped` по `graceEndsAt`, хотя в тот же `tick` пришёл сигнал звучащей цели и верный ответ — `recording` строкой 8. **Вход обязан быть построен так, чтобы фазы давали разные исходы; на всяком другом наборе пункт зелен по построению у обоих порядков** |

### О. Фейк — К87, К88

| К | Вход | Ожидаемый ответ | Чем проверяется | Различающий вектор — какую неверную реализацию красит |
| -- | -- | -- | -- | -- |
| **К87** | `DomainTestKit.FakeSessionCoordinator`, используемый тестами **фасада** (C-016), а не машины | фейк реализует `SessionCoordinator` поверх заданного тестом состояния: задаются наборы `SessionSnapshot` и `SessionPrompt`; **любой** `SessionChange` проталкивается в поток `changes()`; **любая** команда заставляется бросить **любую** `SessionError`; все вызванные команды с аргументами записываются и отдаются тесту списком. Слово «любой» верно дословно: фейк **вправе** отдать снимок, которого верная машина не отдаст — например `recordingId == nil` в состоянии `recording` | `Д` над самим фейком. **Пункт условен целиком: `FakeSessionCoordinator` в дереве `57a2ca3` — ноль объявлений** (§6), и подавать вход нечему | фейк, **приводящий снимок к инвариантам машины** — запрещающий `recordingId == nil` в `recording` зажатием или `precondition`: он красен на этом входе. **Цена его названа контрактом: ветку «фасад пережил негодный вход» не проверить тогда ничем**, то есть дефект фейка снимает проверку у чужого модуля |
| **К88** | тексты реализации машины и её собственных тестов: `Packages/Core/Sources/DomainCore/` и `Packages/Core/Tests/DomainCoreTests/` | **ноль** вхождений `FakeSessionCoordinator` в **обоих**: реализация машины и её тесты не ссылаются на собственный фейк ни одной строкой — ни как на оракул, ни как на образец | `Й` с **двумя** областями, названными порознь: `Sources/DomainCore/` и `Tests/DomainCoreTests/`. Проверяется **след**, а не запрет на вывод: запрет адресован читателю и критерием быть не может | реализацию тестов машины, **взявшую фейк оракулом** — сравнивающую ответ машины с ответом фейка: она красна следом. **Названная граница: сегодня пункт зелен по построению** — `FakeSessionCoordinator` в дереве не объявлен, и ноль вхождений будет у **любой** реализации, включая ту, которую пункт запрещает. Различающая сила появляется у него ровно в тот день, когда фейк заведут (§5, строка 6) |