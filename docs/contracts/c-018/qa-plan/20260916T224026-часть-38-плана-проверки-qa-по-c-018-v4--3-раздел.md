**Часть 3/8 плана проверки QA по C-018 v4 — §3, раздел Ж: таблица переходов §7, критерии К29—К46 вместе с К89 и К90.** Головная часть 1/8 с §0—§2 и оглавлением лежит первым комментарием этой ветки.

### Ж. Таблица переходов §7 — по пункту на строку, в порядке таблицы: К29, К89, К90, К30—К46

**Порядок пунктов здесь — порядок строк таблицы, а не порядок номеров критериев,** потому что строки 1а и 1б стоят в таблице второй и третьей, а их критерии заведены дельтой с номерами 89 и 90 (номера существующих пунктов не двигаются — §8 правил). **Форма всех восемнадцати одна:** подаётся вход, на котором условие строки истинно, — переход обязан наступить; и вход, на котором оно ложно хотя бы одной клаузой, — переход обязан **не** наступить.

| К | Вход | Ожидаемый ответ | Чем проверяется | Различающий вектор — какую неверную реализацию красит |
| -- | -- | -- | -- | -- |
| **К29** (стр. 1) | событие `e`, на котором условие §9.1 истинно **целиком** — `isAllDay == false`, `isCancelled == false`, живой сессии нет, хранимый `MeetingStatus` не терминален, — при `now < armAt`. Отрицательные, по одному на клаузу: **(а)** `isAllDay == true`; **(б)** `isCancelled == true`; **(в)** у встречи есть живая сессия; **(г)** статус `ready`; **(д)** `failed`; **(е)** `skipped`; **(ж)** `now ≥ armAt`. Плюс различающий издания v3: команда `skip` → `stop()` → `start(now:)` **внутри** окна до `graceEndsAt` → `tick` | в `scheduled` сессия заводится **только** на положительном; на (а)—(е) не заводится **ни одна и ни в каком состоянии**; на (ж) заводится, но **не строкой 1** — строкой 1а либо 1б (К89, К90); на векторе перезапуска **не заводится заново** | `Д` + `Е` (счёт снимков) + `Ж` (условие `Н`: заведение читает хранилище, а не память) | реализацию, **помнящую «была ли у встречи сессия» в памяти запуска**: она зелена на (в) и красна на (г), (д), (е) и на векторе перезапуска. **По редакции v2 контракта она была верной** — вектор красит прежнее правило, а не описку. Отдельно: реализацию, не читающую `isAllDay`, — красна на (а) |
| **К89** (стр. 1а) | то же условие §9.1 целиком, при **`armAt ≤ now < e.start`**; границы подаются точно — `now == armAt` и `now == e.start − ε`. Отрицательные: `now < armAt` (заводит строка 1) и `now ≥ e.start` (заводит 1б), и по вектору на каждую клаузу §9.1. **Пути подачи — три, и все три обязательны:** `CalendarChange`; `tick(now:)` по содержимому хранилища; `start(now:)` (§10, перечень Б, встреча в `armed`) | сессия заводится **сразу в `armed`**, минуя `scheduled`, и перехода строкой 2 не происходит **ни одного**: в потоке снимков ровно **один**, и его `state == armed`. `armAt` в прошлом законен и отказом не является. Сроки считаются от `e.start` и от момента заведения не зависят ни на секунду. При `.ask` и прошедшем `askAt` спрос поднимается в момент заведения (К93) | `Е` — **условие `П`**: подписка на `changes()` заводится **до** заведения, включая подписку до `start(now:)`; + `Д` для сроков | реализацию, **заводящую в `scheduled` и тут же переводящую строкой 2**: конечным состоянием она неотличима от верной — обе дают `armed`, — и красна **только** последовательностью потока, двумя снимками вместо одного. **Это довод за способ `Е` и за условие `П`**, а не оговорка. Отдельно: реализацию, считающую сроки **от момента заведения**, — красна на `graceEndsAt`, который уезжает вперёд на `now − e.start` |
| **К90** (стр. 1б) | то же условие §9.1 целиком, при **`e.start ≤ now ≤ graceEndsAt`**; границы точно — `now == e.start` и `now == graceEndsAt`; отрицательные — `now < e.start` (заводит 1а) и **`now > graceEndsAt`** (не заводит ничто). Пути подачи — те же три | сессия заводится **сразу в `awaitingSignal`**, минуя `scheduled` и `armed`; переходов строками 2 и 7 не происходит ни одного. При `now > graceEndsAt` **не заводится ни одна сессия ни в каком состоянии**, и `plan(now:)` такой строки не отдаёт (К71). Сессия, заведённая посреди созвона, находит цель в снимке подписки и уходит в `recording` строкой 8, не дожидаясь `signalTtlSeconds` (К15) | `Е` (условие `П`) + `Д` | две порознь: реализацию, **заводящую при `now > graceEndsAt`** («окно ещё считается открытым, раз grace на границе»), — красна прямо; и реализацию, **проводящую сессию через `armed`**, — красна последовательностью потока. **Граница названа, а не «исправлена»: на `now == graceEndsAt` истинны сразу 1б и строка 9, и порядок между заведением и проверкой сроков внутри одного `tick` контракт называет только для пришедшего события** (находка 7 дельты). Пункт поэтому требует исхода, от порядка **не зависящего**: сессия существует (инв. 17), и без цели уходит в `skipped` **не позже первого `tick`, следующего за заведением** |
| **К30** (стр. 2) | `tick(now:)` с `now`: меньше `armAt`; равным `armAt` **точно**; между `armAt` и `graceEndsAt`; равным `graceEndsAt` **точно**; и **больше** `graceEndsAt` | переход наступает при `armAt ≤ now ≤ graceEndsAt` и **только** на этом отрезке; **обе границы включительны и проверяются порознь**; раньше `armAt` не наступает; **при `now > graceEndsAt` строка 2 не срабатывает вовсе**, и сессия уходит в `skipped` строкой 3 | `Д` | реализацию **без верхней границы** — редакцию v1 дословно: она зелена в четырёх моментах из пяти и красна ровно при `now > graceEndsAt`, где обязана сработать строка 3. **Именно эта реализация делала третью клаузу строки 3 мёртвой на всяком наборе настроек с неотрицательными сроками**, и вектор — единственное место плана, где она краснеет |
| **К31** (стр. 3) | три условия порознь — команда `skip(meetingId:now:)`; событие отменено (`isCancelled == true`) или удалено; `tick` с `now > graceEndsAt`. Плюс два граничных: `now == graceEndsAt` и `now` внутри `[armAt, graceEndsAt]`. Плюс различающий издания v3: после `skipped` командой — перезапуск внутри окна | переход наступает на каждом из трёх порознь. При `now > graceEndsAt` из `scheduled` побеждает **строка 3**, а не 2, и сессия уходит в `skipped` **за один** `tick`, а не за три. Знак строгий: при `now == graceEndsAt` строка 3 **не** срабатывает, срабатывает строка 2. Асимметрия со строкой 9 (`≥`) намеренна, и «выравнивать» её запрещено — критерий подаёт оба знака порознь. На векторе перезапуска сессия заново не заводится | `Д` + `Ж` (условие `Н`) | две порознь: реализацию, **выровнявшую знак до `≥`**, — красна ровно при `now == graceEndsAt`: уйдёт в `skipped` вместо `armed`, и ни один другой момент её не ловит; и реализацию **без верхней границы строки 2** — красна при `now > graceEndsAt` тремя `tick` вместо одного. **Две неверные реализации, два разных момента, один пункт** |
| **К32** (стр. 4) | три условия порознь — команда `skip`; ответ `.skip` на поднятый спрос; событие отменено или удалено. Плюс различающий издания v3: ответ `.skip` → `stop()` → `start(now:)` внутри окна до `graceEndsAt` → `tick` | переход наступает на каждом из трёх; `now ≥ graceEndsAt` условием строки 4 **не является** — из `armed` по сроку grace переход не идёт, потому что раньше срабатывает строка 7. На различающем: сессия заново не заводится **ни строкой 1, ни 1а, ни 1б** | `Д` + `Ж` (условие `Н`: `setStatus` со `skipped` вызван прежде публикации — инв. 18) | реализацию, **не записывающую исход ответа `.skip` в хранилище прежде публикации снимка**: она зелена на всех трёх условиях внутри одного запуска и красна на перезапуске — заведение читает хранилище и не находит там `skipped`. Вектор связывает К32 с К81 и К91: **дефект устройства один, а краснеет он в трёх местах плана порознь** |
| **К33** (стр. 5) | `CalendarChange`, сдвигающий событие вперёд так, что `now < armAt` **нового** окна; и сдвиг вперёд, при котором `now ≥ armAt` нового окна | на первом сессия возвращается в `scheduled` и взводится заново по новому окну, `sessionId` не меняется (К7); на втором **не возвращается** | `Д` + `Е` | реализацию, возвращающую в `scheduled` **на всяком** сдвиге вперёд (условие прочитано как «событие подвинулось»): она зелена на первом и красна на втором — где новое `armAt` уже прошло и сессия обязана остаться взведённой |
| **К34** (стр. 6) | из `armed`, **до `e.start`**: все три клаузы истинны — есть звучащая цель §5.3, цель не занята §7.1, запись разрешена политикой §8.2. И три отрицательных, каждый снимает **ровно одну** клаузу: цели нет; цель занята идущей записью другой сессии; политика запрещает (`.manual`, либо `.ask` без ответа) | переход наступает **только** при всех трёх истинных; каждый отрицательный его снимает. Переход **до `e.start` разрешён** — это ранний вход, а не дефект | `Д` | две порознь: реализацию, **не проверяющую занятость цели**, — красна на втором отрицательном, и это единственный вектор К34, который её ловит (К47 ловит её же с другой стороны); и реализацию, **запрещающую ранний вход** («до начала события писать нечего»), — красна на положительном, потому что вход подаётся строго до `e.start` |
| **К35** (стр. 7) | `tick` с `now` меньше `e.start`, равным `e.start`, больше `e.start` — при **отсутствии** звучащей цели; и отдельно: `now ≥ e.start` **при наличии** цели и разрешающей политике | переход наступает при `now ≥ e.start`; при наличии цели и разрешающей политике раньше него срабатывает **строка 6**, и сессия уходит в `recording`, а не в `awaitingSignal` | `Д` | реализацию, читающую строки **не по порядку таблицы** (например, сроковые прежде сигнальных): она зелена на трёх векторах без цели и красна на четвёртом — сессия окажется в `awaitingSignal` вместо `recording`, потеряв начало созвона |
| **К36** (стр. 8) | те же четыре вектора, что у К34, но из состояния `awaitingSignal` | условие **то же самое**, что в строке 6, дословно: три клаузы, каждая снимающая. Один и тот же набор подаётся в оба состояния и требует **одинакового** ответа | `Д` | реализацию, у которой условие строки 8 **разошлось** со строкой 6, — например, в `awaitingSignal` политика не проверяется («сигнал пришёл, значит человек уже в созвоне»): она зелена на всех векторах К34 и красна на отрицательном «политика запрещает» здесь. **Два места, где написано одно условие, расходятся молча, и ловит это только парная подача** |
| **К37** (стр. 9) | четыре условия порознь — команда `skip`; ответ `.skip`; событие отменено или удалено; `tick` с `now ≥ graceEndsAt` **и отсутствием** звучащей цели. Плюс различающий по цели: `now ≥ graceEndsAt` **при наличии** цели. Плюс различающий издания v3: `skip` либо `.skip` → `stop()` → `start(now:)` внутри окна → `tick` | на первых четырёх переход наступает; на различающем по цели — **не** наступает, и сессия уходит в `recording` строкой 8, если политика разрешает. Знак `≥` проверяется **в сам момент** `graceEndsAt`: момент принадлежит сессии, и решение по grace принимается **в** названные пятнадцать минут. На векторе перезапуска сессия заново не заводится | `Д` | две порознь: реализацию **без клаузы «звучащей цели нет»** — красна на различающем по цели: уводит в `skipped` созвон, который в эту секунду звучит; и реализацию **со строгим `>`** — красна ровно в момент `graceEndsAt`, где решение обязано быть принято в срок, а не после него |
| **К38** (стр. 10) | два условия порознь — условие остановки §8.4 (К57); команда `stopRecording(recordingId:now:)`. Отдельно: `tick` с `now > e.end` при **актуальной** цели | переход наступает на каждом из двух. `e.end` в условии строки 10 **не участвует** — вектор «окно события кончилось, цель актуальна» перехода не даёт | `Д` | реализацию, останавливающую запись **по концу окна события** (`e.end` внесён в условие строки 10 как «встреча кончилась»): она зелена на двух положительных и красна на третьем векторе. Тот же вектор стоит в К56 со стороны §8.4 — **пункты парны намеренно: один проверяет строку, другой правило** |
| **К39** (стр. 11) | `CaptureEvent.failed(_)` в состоянии `recording` | сессия уходит в `failed`, **минуя `stopping`**; манифест не ожидается и не требуется; `recordingId` сохраняется (К3) | `Е` (в потоке между `recording` и `failed` **ни одного** снимка `stopping`) + `Ж` (условие `Н`) | реализацию, **проводящую отказ через `stopping`** («выход из записи всегда через остановку»): конечным состоянием она неотличима — обе дают `failed`, — и красна **только** последовательностью потока. **И она же красна в К67**: токен, отпускаемый «при выходе из `stopping`», на этом пути у неё отпускается, а у верной реализации путь `stopping` не проходит вовсе — два пункта ловят обратные ошибки одного места |
| **К40** (стр. 12) | **(а)** получен `CaptureEvent.stopped(manifest)` **и** запись сохранена репозиторием C-010; **(б)** `stopped(manifest)` получен, а сохранение **не** состоялось (фейк репозитория бросает) | переход наступает **только** на (а) — обе клаузы обязательны; на (б) сессия остаётся в `stopping`. При входе в `processing` ставится **первая** задача цепочки, `transcode(recordingId)` (К62) | `Д` + `Ж` (условие `Н`: порядок «сохранение → вход → постановка») | реализацию, входящую в `processing` **по одному `stopped`**: она зелена на (а) и красна на (б) — встреча уедет в обработку записи, которой в хранилище нет, и цепочка встанет на первой же задаче, читающей каталог |
| **К41** (стр. 13) | два условия порознь — `stop()` захвата бросил; `CaptureEvent.failed(_)` в состоянии `stopping` | переход наступает на каждом; задача цепочки при этом **не** ставится | `Д` + `Ж` (условие `Н`: ноль `submit` за прогон) | реализацию, ставящую первую задачу цепочки **при всяком выходе из `stopping`** (постановка привязана к выходу, а не к входу в `processing`): она зелена на состоянии и красна журналом очереди — одна лишняя задача на запись, которой нет |
| **К42** (стр. 14) | `JobEvent.succeeded` задачи **`attribute` этой записи**; и различающие — `succeeded` задач `transcode`, `transcribe`, `diarize` этой же записи; `succeeded` задачи `attribute` **чужой** записи | переход в `ready` наступает **только** на своей `attribute`; на трёх промежуточных ставится следующая задача цепочки, а состояние не меняется; на чужой `attribute` **не происходит ничего** | `Д` + `Ж` (условие `Н`) | две порознь: реализацию, входящую в `ready` **по `succeeded` любой задачи**, — красна на `transcode` (сессия уедет в `ready` с пустым транскриптом); и реализацию, **не сверяющую запись**, — красна на чужой `attribute`, и это единственный вектор плана, который сверку записи проверяет |
| **К43** (стр. 15) | `JobEvent.failed(willRetry: false)` и `JobEvent.cancelled` — по вектору на каждую из **четырёх** задач цепочки; и различающие — `failed(willRetry: true)`, `blocked` | переход в `failed` наступает на `failed(willRetry: false)` и `cancelled` любой из четырёх; на `failed(willRetry: true)` и `blocked` состояние **не меняется** (инв. 20) | `Д` + `З` (перебор по случаям `JobEvent`) | реализацию, входящую в `failed` **по всякому `failed`**: она зелена на восьми векторах и красна на `willRetry: true` — встреча уедет в `failed` при отказе, который очередь собирается повторить, и повторный успех её уже не оживит (К66) |
| **К44** (стр. 16) | два условия порознь — команда `startRecording(meetingId: nil, now:)`; ответ `.record` на спрос §8.6. Плюс: то же при **занятой** цели | заводится сессия с `origin == .adHoc`, `meetingId == nil`, назначенным `recordingId`, в состоянии `recording`; условие §7.1 проверяется **и здесь** — при занятой цели команда бросает `alreadyRecording` (К48) | `Д` | реализацию, **не проверяющую занятость цели для ad-hoc** («у ad-hoc нет встречи, значит и спорить не с кем»): она зелена на двух положительных и красна на третьем — двумя сессиями в `recording` с равным `target?.appKey`, то есть двумя tap-ами на одну группу и двумя манифестами одного звука |
| **К45** (инв. 2, порядок) | состояния, где истинны **две и более** строки, и отдельным видом — где пересечение было и **снято**: **(i)** `scheduled`, `now > graceEndsAt` — пересечения больше нет: строка 2 ложна верхней границей, истинна только 3; **(ii)** `armed`, есть цель и `now ≥ e.start` — строки 6 и 7; **(iii)** `armed`, ответ `.skip` и одновременно есть цель — строки 4 и 6; **(iv)** `recording`, одновременно условие §8.4 и `CaptureEvent.failed` — строки 10 и 11; **(v)** заведение при `now < armAt`, при `armAt ≤ now < e.start` и при `e.start ≤ now ≤ graceEndsAt` — истинна **ровно одна** из 1, 1а, 1б на каждом отрезке | в (ii), (iii), (iv) побеждает строка с **меньшим номером**: `recording`, `skipped`, `stopping`. В **(i)** сессия уходит в `skipped` строкой 3, и проверяется, что строка 2 **не побеждает, потому что ложна**, а не потому, что уступила. В **(v)** три строки взаимно исключают друг друга по `now` — это разбиение отрезка, и порядок между ними безразличен | `Д` | реализацию, читающую строки **не в порядке таблицы**, — красна на (ii), (iii), (iv) порознь тремя разными исходами; и реализацию **без верхней границы строки 2** — красна на (i). **Названная неполнота, и она моя находка, а не оговорка критерия:** пересечений в таблице больше пяти. Мой прогон по восемнадцати строкам даёт ещё **четыре**, которых К45 не подаёт — **4/5, 5/6, 12/13, 14/15**, — и зелёный пункт о порядке на них не доказывает ничего (§5, §8, строка 3) |
| **К46** (инв. 2, тотальность и исключение) | **вид 1 — тотальность живых переходов:** для каждого из девяти состояний каждый вход §2, **не названный** ни одной строкой §7 для этого состояния (полный декартов перебор «состояние × вид входа» за вычетом названных). **Вид 2 — исключение издания v4:** `start(now:)` подаётся **на всяком содержимом хранилища** — перебором по `RecordingStatus` и по `MeetingStatus` разом, включая пары, где перечни А и Б расходятся (К79) | **вид 1:** состояние **не меняется** и ошибки **не даётся** — ни `throw`, ни `fatalError`, ни publish в `changes()`; отображение «состояние × вход → состояние» тотально; иных переходов, кроме **восемнадцати** строк, не наблюдается. **вид 2:** сессии **вне таблицы** заводятся **ровно двумя способами**, и оба — в `start(now:)` по §10, перечню А: вход в `processing` и вход в `failed`; **третьего нет ни одного**, и перебор показывает это отсутствием. Живых переходов исключение не касается: ни один `tick` восстановительного заведения не повторяет | `З` (перебор по перечислениям состояний, видов входа и обоих статусов) + `Е` (условие `П`: поток молчит на всём переборе вида 1) + `Д` | три порознь: реализацию, **бросающую на неназванном входе**, — красна на виде 1 (её пишут первой: «неожидаемый вход — это ошибка»); реализацию, **заводящую сессию третьим способом** — например, по `PowerEvent.didWake` дочитывающую хранилище, — красна на виде 2; реализацию, **не заводящую в §10 вовсе** (исполнившую инвариант 2 по букве прежних редакций), — красна на виде 2 **отсутствием двух**. **Граница названа: «третьего входа нет» проверяется перебором по пяти названным местам** (К92 (iii)), и шестое место, если оно есть, этим перебором не ловится (§5) |