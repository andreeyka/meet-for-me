**Часть 8/8 плана проверки QA по C-018 v4 — §7 чего зелёный прогон не доказывает, §8 строки, §9 что проверено и сошлось, §10 арифметика.** Головная часть 1/8 с §0—§2 и оглавлением лежит первым комментарием этой ветки.

---

## §7. Отдельной строкой: что зелёный прогон этого плана **не** доказывает

1. **Зелёный К91 не доказывает, что повторяющаяся встреча будет записана больше одного раза.** Предпосылка инварианта 24 — из двух половин; издание C-005 v4 закрыло одну (инвариант 2: два вхождения одной серии равного `DedupKey` не получают ни при какой ветви) и **отказалось ловить вторую** дословно: «серия, отданная одним событием вместо вхождений, приходит сюда как одно событие и становится одной встречей… отличить такой плагин от плагина, у которого встреча действительно одна, здесь нечем ни одним полем». **На таком плагине все 93 пункта зелены, инвариант 24 исполнен дословно, а планёрка записана один раз.** Владелец второй половины — C-006, и ни C-018, ни C-005, ни этот план её не проверяют.
2. **Зелёный К28 не доказывает, что порога по `estimate` нет.** Он доказывает, что **этот** порог не сработал: реализация с порогом выше 0,937792 зелена и на нём. Числовой границы «любой порог, кроме единицы» вектор не покрывает — он покрывает названный.
3. **Зелёные текстовые пункты доказывают отсутствие написания, а не отсутствие поведения** — §5 п. 7 поимённо.
4. **Зелёный К81 (iii) может быть зелёным по времени, а не по существу.** Это единственный пункт плана, чей ответ есть невозникновение события, и наблюдается он **пределом ожидания** (условие `Р`). Предел, взятый короче задержки фейка, красит верную реализацию; взятый длиннее — зеленит неверную, если та вернула управление позже записи по случайности расписания. **Довод против лучшего способа: заданный тестом момент отсутствия возврата не даёт, потому что отсутствие — не значение.** Цена названа: один нестабильный пункт из 93.
5. **Зелёный прогон `Core (Linux)` сегодня не говорит об этом плане ничего.** Субъекта в дереве нет: `SessionCoordinator` — 0 объявлений. Работа зелена и до принятия плана, и после, и будет зелена на дереве, в котором ни один из 93 пунктов не исполняется. **Первый осмысленный прогон этого плана возможен не раньше, чем в дереве появятся шесть портов, семнадцать типов границы и четыре фейка** (§6).
6. **Зелёный К45 не доказывает порядок строк на четырёх пересечениях из семи** (§5 п. 3), зелёный К46 — отсутствие шестого места заведения (§5 п. 5), зелёный К88 сегодня — ничего (§5 п. 6), зелёный К71 — сортировку по `armAt`, а не по `start` (§5 п. 2).
7. **Различающая сила названных векторов замером не подтверждена ни у одного пункта, и это сказано прямо.** Способ, которым она подтверждается, известен и записан в правилах (§3, «требование доказать различающую силу вектора несёт санкцию на мутацию с собой»; служебная ветка `qa/mutation-<sha>`, PR с маркером, три прогона в одну разницу). **Здесь он не применён, и довод не в нехватке санкции, а в отсутствии предмета: мутировать нечего** — реализации машины в дереве нет ни строкой, а мутация верной реализации есть единственный способ доказать, что вектор ловит. **Отсюда прямое следствие для выдачи задачи DEV: замер силы векторов этого плана — отдельная работа, и она возможна только после первой зелёной реализации.** Ровно так это и стоило проекту в [MEE-280](https://linear.app/easypto/issue/MEE-280), где из одиннадцати векторов К75/К76 чужой серии **один** ловил свою функцию в одиночку.

---

## §8. Строки — шесть. У каждой владелец, место, условие снятия и срок

**Задач не заведено, номеров IR не присвоено, PR не открыт** (§6 постановки). **Перечень MEE-277, контракт MEE-276, MEE-75, MEE-126, код, `docs/`, `ci.yml` — не тронуты ни символом.**

### Строка 1 (дороже прочих). §6 п. 10 объявлен ЗАКРЫТЫМ, а предпосылка К91 закрыта наполовину

**Что вижу, дословно с обеих сторон.** П. 11 дельты `Г` (21:39:44Z): «предпосылка инварианта 24 C-018 **обеспечена**… вектор **свидетельствует**», §6 п. 10 — «**ЗАКРЫТ** изданием C-005 v4». C-005 v4, «Что вне контракта», строка, **заведённая тем же изданием**: стороны не имеют права предполагать, что «этот контракт **ловит** плагин, который ту обязанность не исполнил: серия, отданная **одним событием** вместо вхождений… отличить такой плагин от плагина, у которого встреча действительно одна, здесь нечем **ни одним полем**». C-005 v4, «Что вне контракта», там же: обязанность плагина — «отдать серию развёрнутыми вхождениями, каждое со своим `externalId` (**C-006**)».

**Отсюда: издание закрыло половину предпосылки, а пункт закрыт целиком.** Закрыта половина «C-005 не сольёт два вхождения» — она теперь **гарантирована** ключом, и прежняя редакция п. 10, стоявшая на C-005 v3, ложна с 21:18Z. **Не закрыта половина «плагин вообще отдаёт серию вхождениями», и C-005 печатает, что её неисполнение не ловит.**

**Цена, если оставить.** Зелёный К91 будет прочитан доказательством продуктового свойства — «еженедельная планёрка записывается каждую неделю», — которого не обеспечивает ни один контракт проекта. **Ловится это только чужой приёмкой, и ровно в тот день, когда пользователь недосчитается записей.** **Владелец:** аналитик (§6 перечня — его зона). **Место:** §6 п. 10 в редакции п. 11 дельты `Г`; смежно — абзац предпосылки в К91. **Условие снятия:** п. 10 назван закрытым **наполовину**, и открытая половина адресована C-006 — либо закрыт совсем, когда появится место, отвечающее за развёртку серии, с названным способом проверки. **Срок:** не позже выдачи задачи на реализацию машины: на ней этот пункт станет зелёным, и после того его прочтут доказательством.

### Строка 2. Постановка не называет четвёртого текста перечня

**Что вижу:** §1 постановки собирает действующее состояние из трёх текстов; в ветке MEE-277 их четыре — п. 11 дельты `Г`, 21:39:44Z, 15 895 знаков, `7614e9ad6147e396`, правящий действующий текст К91 и §6 п. 10. **Цена:** план на К91, написанный по трём названным текстам, нёс бы снятую редакцию критерия — тот самый класс, за который §8 правил уже заплатил четырежды. **Владелец:** РП (постановки — его зона). **Место:** §1 постановки MEE-288, таблица «Что читать». **Условие снятия:** четвёртый текст назван в постановке либо в приёмке этой задачи. **Срок:** приёмка MEE-288 — позже он не нужен, работа уже сделана по четырём.

### Строка 3. К45 проверяет порядок строк на трёх пересечениях из семи

**Что вижу:** мой прогон по восемнадцати строкам §7 даёт пересечения, которых К45 не подаёт: **4/5, 5/6, 12/13, 14/15** (разбор — §5 п. 3). **Дефект базовый, изданиями v2—v4 не заведённый:** в базовой редакции пересечений было названо четыре, дельта переписала (i) и добавила (v), и ни одна из правок этих четырёх не касалась. **Цена:** дороже прочих пара **5/6** — `armed`, есть звучащая цель, и в тот же `tick` событие сдвинулось вперёд за `armAt`: верный исход `scheduled` (строка 5 меньше), и реализация, начавшая запись, зелена по всему плану. **Владелец:** аналитик (перечень). **Место:** К45, перечисление (i)—(v). **Условие снятия:** К45 подаёт все пересечения таблицы либо называет границей, что проверяет выборку и какую. **Срок:** не позже выдачи задачи на реализацию машины.

### Строка 4. Ни один критерий не решает, отдаёт ли `plan(now:)` строку встречи с терминальным `MeetingStatus`

**Что вижу:** §9.1 говорит «`plan(now:)` возвращает `ScheduledArm` для **всех таких** событий», и входит ли в «такие» клауза о хранимом `MeetingStatus`, текст не разделяет; К71 требует «все события с `now ≤ graceEndsAt`», К74 принимает исход «терминальная, если уже ушла», и обе реализации проходят оба (разбор — §5 п. 4). **Цена:** инвариант 15 привязывает `nextDeadline(now:)` к ближайшему `armAt` из `plan`; реализация, кладущая туда терминальные встречи, будит машину к срокам, которые сессии не получат, — и К73 её не ловит, потому что ограничивает `nextDeadline` сверху, а не снизу. **Владелец — двое, и это не уклонение, а два разных места:** **архитектор C-018** — неразделённое «все такие» в последнем пункте §9.1; **аналитик** — К71, чей Ответ утверждает «все события с `now ≤ graceEndsAt`» без клауз §9.1. **Условие снятия:** §9.1 говорит, входит ли терминальный статус в фильтр `plan`, **либо** К71 называет фильтр поимённо. **Срок:** не позже выдачи задачи на реализацию машины. **Изданием v5 это не является и его не просит** (стоп-правило приёмки [MEE-281](https://linear.app/easypto/issue/MEE-281) §8): противоречием контракта самому себе неразделённое «такие» не является — оно неполно, а не двояко в ответах.

### Строка 5. Число «критериев, содержащих числовое сравнение с `estimate` — 0» не воспроизводится

**Что вижу:** §10 дельты `Г` печатает **0**; мой прогон по обоим телам даёт **два** вхождения `estimate > 0,9`, оба в базовой редакции, и одно из них — **в Ответе К28**, второе — в §0.1, в абзаце, который и утверждает, что таких критериев ноль. **Верно при чтении «сравнение как условие перехода»; ложно как напечатано.** **Цена прямая — нулевая**, косвенная — одна перепроверка у следующего счётчика. **Владелец:** аналитик (перечень). **Место:** §10 дельты `Г`, строка «критериев, содержащих числовое сравнение с `estimate`»; §0.1 базовой редакции, последняя фраза. **Условие снятия:** число названо по своему предмету — «сравнений `estimate` с числом в условии перехода — 0», — либо напечатано 1 с адресом К28. **Срок:** ближайшая дельта по перечню.

### Строка 6. Перечисление, на котором стоит «новых средств не требуется ни одного», не называет фейка `AudioCapturePort`

**Что вижу:** §12 дельты `Г` и §3 постановки называют «фейки репозиториев C-010 и C-013, `FakePowerPort`, `FakeProcessMonitorPort`, `FakeCalendarPort` и заданные тестом моменты». **Фейка `AudioCapturePort` нет ни в одном из двух перечислений**, а без него не подаются К38—К41, К59, К77 и К85: `CaptureEvent.stopped/failed/systemSilent`, `recover(directory:)` и `stop()` захвата приходят только оттуда. **Сам контракт его называет** («фейке `AudioCapturePort` (C-004)»), то есть утверждение «новых средств не требуется» от этого не рушится — **рушится перечисление, которым его проверяют**. **Цена:** постановка задачи DEV, собранная по этому перечислению, недосчитается одного фейка из пяти, и семь пунктов плана окажутся без входа. **Владелец:** аналитик (§12 дельты) и РП (§3 постановки) — по своему месту каждый. **Условие снятия:** перечисление называет фейк `AudioCapturePort`. **Срок:** не позже выдачи задачи на реализацию машины.

---

## §9. Что проверено и **сошлось** — называю тем же счётом, потому что «сошлось» есть такой же результат прогона, как расхождение

**§8 постановки назвал три места, которых не проверял. Проверены все три, и ниже — исход каждого.**

1. **Двенадцать правленых критериев, не сличённых постановкой построчно (К29, 30, 31, 32, 37, 45, 67, 73, 74, 77, 78, 81), сличены мной с базовой редакцией и с действующим контрактом.** **Неполной правки не нашлось ни одной:** у каждого из двенадцати правка отвечает тому изданию, которое дельта называет, и Ответ сходится с действующим текстом контракта дословно — проверено чтением строки таблицы §7 либо инварианта целиком, вместе с субъектом. **Найдено другое, и оно не о правке:** К45 неполон **базово**, а не правкой (строка 3), и правки его это не касалось ни одним словом.
2. **Таблицы покрытия §7.1—§7.3 дельты пересчитаны — все три, и все три сходятся.** §7.1: **24** строки, номера 1…24 подряд, **инвариантов без критерия — ноль**. §7.2: **18** строк, 1, 1а, 1б, 2…16, критерии К29—К46 плюс К89, К90, пропусков ноль. §7.3: разбор раздела «Что вне контракта» — **24 стрелки на 24 буллета, одна к одной, в том же порядке**, дубликатов нет. **И счёт самого контракта, снятый моим прогоном, воспроизводит дельту до знака:** инвариантов **24** (номера подряд), строк §7 — **18**, подразделов §8 — **7** (8.1…8.7), буллетов «Что вне контракта» — **24**, из них открываются полужирным **21**, инлайн-кодом **3**. **Находка 1 дельты `Г` воспроизвелась целиком, включая поправку п. 10: `24 − 3 = 21`.**
3. **§10 «Арифметика» пересчитан построчно.** Сходятся: 93 критерия К1—К93 подряд, пропусков ноль; 93 из 93 — `Core (Linux)`; `Core + Mac` — 0; ручных сценариев 1; инвариантов 24 из 24; строк 18 из 18; подразделов §8 — 7 из 7; «Что вне контракта» 24 из 24; заведений вне таблицы 2 из 2; правится 18 / заводится 5 / было 88 → стало 93; не тронуто 70, и `70 + 18 = 88` без пересечений; меток 1 / 4 / **25** (`29 − 4`). **Не сошлось одно: «числовое сравнение с `estimate` — 0»** (строка 5). **И одно устарело этой записью: «план — не заведён»** (§0.6).
4. **Утверждение аналитика «новых средств проверки не требуется ни одного» проверено** — исход в §0.4 тремя строками, и перечисление под ним неполно (строка 6).
5. **Перечисление новых векторов из §3 постановки проверено и сходится целиком:** три пути подачи × два состояния заведения (К89, К90) — оба критерия называют все три пути; перебор `start(now:)` на два заведения и отсутствие третьего (К46, К92); перебор девяти `MeetingStatus` (К78 — **и К91, чего §3 не называет**) и четырёх `RecordingStatus` (К77) с порядком «сперва запись, потом `processing`»; вектор перезапуска на К29, К31, К32, К37 и К91; порядок возврата команды и ветвь падения (К81); четыре недошедшие задачи и **пять** мест третьего входа (К92); три ветви по исходу спроса (К80). **Единственная поправка — та, что в скобке: перебор девяти значений живёт в двух критериях, а не в одном.**
6. **Расхождение §0 дельты `Г` («оговорок пять, а не шесть») подтверждаю своим прогоном:** у К78 базовой редакции нет ни оговорки о собственной неполноте, ни ссылки на §6 перечня — проверено чтением К78 целиком. Оговорки снимаются у К29, К67, К73, К74, К77 — пять.
7. **Границы постановки исполнены дословно, и по каждой сказано, что я делал:** перечень [MEE-277](https://linear.app/easypto/issue/MEE-277) — читан целиком, **не тронут ни символом**; контракт [MEE-276](https://linear.app/easypto/issue/MEE-276) — читан целиком, **не тронут ни символом**, издания не прошу; [MEE-9](https://linear.app/easypto/issue/MEE-9) (C-005 v4) — **читан** ради строки 1, не тронут; [MEE-126](https://linear.app/easypto/issue/MEE-126) — читан **как образец формы**, не тронут ни символом, и ни одного его факта я не перенёс; [MEE-75](https://linear.app/easypto/issue/MEE-75) — **не открывал ни на чтение, ни на запись**; C-010 ([MEE-18](https://linear.app/easypto/issue/MEE-18)) и C-013 ([MEE-21](https://linear.app/easypto/issue/MEE-21)) — **не открывал**, и всё, что о них сказано, снято прогоном по **дереву**, а не по их текстам, и названо как замер дерева; описание MEE-288 — **не правлено**, план лежит комментарием; **код, `docs/`, `.github/workflows/ci.yml` — ни символом**, `git status` чист, веток не заводил, коммитов нет, PR не открывал. Дерево читал: `git fetch`, `git rev-parse`, `git log`, `git status`, `docs/process.md`, `Packages/` на чтение.

---

## §10. Арифметика — каждое число воспроизводимо, и у каждого назван способ

| Что | Числом | Способ |
| -- | -- | -- |
| критериев в перечне | **93**, К1—К93 подряд, пропусков 0 | прогон по трём телам MEE-277, выгруженным отдельным запросом |
| пунктов плана | **93**, по одному на критерий, номера совпадают | прогон по этому тексту после записи |
| пунктов, не покрытых планом | **0** | то же |
| место исполнения: `Core (Linux)` / `Core + Mac` / вручную | **93 / 0 / 1** | пометки критериев: 89 в базовой, 23 в дельте, 1 в п. 11 |
| способов плана / условий исполнимости | **8 / 6** | §1, §2 |
| меток взято этой работой / занято в серии / осталось односимвольных | **14 / 18 / 10**, первая свободная — `У`, `К` пропущена с доводом | прогон по трём местам серии, `29 = 33 − 4` |
| пунктов, условных по условиям §2: упоминаний / различных номеров | **34 / 26** | §4 |
| пунктов, не исполнимых до появления средства | **2** (К87, К13) | §6 |
| средств требуется / есть в дереве / из них неполны | **8 / 2 / 2** | `origin/main@57a2ca3` после `git fetch`, область `Packages/` |
| портов, которых нет / типов границы, которых нет / фейков завести | **6 / 17 / 5** | тот же прогон; пятый фейк — `FakeSessionCoordinator` |
| объявлений `SessionCoordinator` / `Scheduler` / `SessionSnapshot` в дереве | **0 / 0 / 0** | тот же прогон |
| клауз, зелёных по построению, названо планом | **8 классов** | §5 |
| строк (находок) | **6** | §8 |
| из трёх мест §8 постановки проверено | **3 из 3**; не сошлось **одно** число | §9 |
| задач заведено / номеров IR присвоено / PR открыто | **0 / 0 / 0** | §9 п. 7 |
| перечень, контракт, MEE-75, MEE-126, код, `docs/`, `ci.yml` тронуто | **0 / 0 / 0 / 0 / 0 / 0 / 0** | `git status` чист; правок в Linear нет |

**Область прогона для проверки этих чисел названа вместе с тем, входит ли в неё сама запись:** текст настоящего комментария, выгруженный отдельным запросом **после** записи, вместе с тремя телами MEE-277 и описанием MEE-276; **сама запись в область входит и свои числа считает по себе.** Числа о дереве сняты **после `git fetch`**, и адрес напечатан рядом: `origin/main` = **`57a2ca3`**.

---

*Generated by [Claude Code](https://claude.ai/code)*