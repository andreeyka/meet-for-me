## Дельта `А`, п. 10 — поправка, внесённая **до** отчёта. Буквы не берёт

**Дописывается в конец, прежние девять пунктов дельты целы посимвольно** (§8 правил проекта, издание 11). **Метки этот пункт не берёт:** буквы обозначают разделы дельт, а не пункты внутри одной — «подразделы внутри дельты нумеруются, а не обозначаются буквами: буквы дороже». Серия C-018 по-прежнему держит одну занятую букву — `А`, следующая свободная — `Б`.

**Издания эта правка не требует, и довод назван по §8: она не меняет ни одного ответа ни на одном входе** — правится утверждение **о** правиле, а не правило. Условие, при котором такая правка законна без издания, исполнено ровно здесь: полное «было → стало» напечатано в проверяемом месте.

### Что было неверно — нашёл собственным прогоном по своему же тексту, а не приёмка

Издание v2 дважды утверждало, что третья клауза строки 3 §7 «не срабатывала **никогда**». **Это шире посылки** (правило 14): она мертва не тождественно, а при `armAt ≤ graceEndsAt`, то есть при `armLeadSeconds + missingSignalGraceSeconds ≥ 0`. **§5.1 того же контракта соотношений между настройками не требует ни одного и говорит это дословно** — значит набор с отрицательным сроком контрактом не запрещён, и на нём клауза срабатывала бы. **Молчание о границе читается как её отсутствие** (правило 9), и здесь оно читалось как «мертва при любых настройках».

**Цена, если бы осталось:** перечень получил бы критерий на утверждение, которое ложно на векторе, законном по §5.1, — и этот вектор либо не был бы подан вовсе, либо покрасил бы верную реализацию. Второе — правило 7 наоборот: красное на верном. **На сам разрез правки это не влияет ни на сколько:** верхняя граница строки 2 снимает зависимость от знака настроек целиком, и это сказано теперь прямо.

### 10.1 §7, абзац под таблицей — последнее предложение

**Было (v2, 19:36:50Z), дословно:**

> **Верхняя граница строки 2 (**`now ≤ graceEndsAt`**) заведена тем же доводом:** без неё третья клауза строки 3 не срабатывала бы **никогда**, потому что строка 2 стоит раньше и истинна на всём `now ≥ armAt`, — и обещание §9.2 не исполнялось бы ни одной верной реализацией.

**Стало (v2, 19:46:01Z), действующий текст целиком:**

> **Верхняя граница строки 2 (**`now ≤ graceEndsAt`**) заведена тем же доводом:** без неё третья клауза строки 3 не срабатывала бы ни разу, потому что строка 2 стоит раньше и истинна на всём `now ≥ armAt`, — и обещание §9.2 не исполнялось бы ни одной верной реализацией. **Граница этого утверждения названа вместе с ним, потому что молчание о ней читалось бы как её отсутствие:** оно верно тогда и только тогда, когда `armAt ≤ graceEndsAt`, то есть когда `armLeadSeconds + missingSignalGraceSeconds ≥ 0`. **Соотношений между настройками этот контракт не требует ни одного (§5.1)**, и потому клауза была мертва не тождественно, а на всяком наборе с неотрицательными сроками, включая умолчания C-016. Верхняя граница строки 2 снимает эту зависимость от знака настроек целиком: строка 2 и третья клауза строки 3 теперь не пересекаются ни при каких значениях.

### 10.2 §«Ломающие изменения против v1», п. 5, клауза «Что чинится»

**Было (v2, 19:36:50Z), дословно:**

> **Что чинится:** третья клауза строки 3 не срабатывала **никогда** — строка 2 стоит раньше и была истинна на всём `now ≥ armAt`, — то есть обещание §9.2 («новый `graceEndsAt` уже прошёл → сессия уходит в `skipped` строкой 3») не исполнялось ни одной верной реализацией.

**Стало (v2, 19:46:01Z), действующий текст целиком:**

> **Что чинится:** третья клауза строки 3 не срабатывала **ни разу при** `armAt ≤ graceEndsAt`**, то есть на всяком наборе настроек с неотрицательными сроками, включая умолчания C-016** — строка 2 стоит раньше и была истинна на всём `now ≥ armAt`, — то есть обещание §9.2 («новый `graceEndsAt` уже прошёл → сессия уходит в `skipped` строкой 3») не исполнялось ни одной верной реализацией.

**Остальной текст п. 5 абзаца ломающих изменений не тронут ни знаком**, и абзац по-прежнему помечен «сохранено: дельта критериев по нему ещё не выдана».

### 10.3 Числа п. 1 дельты, которые эта правка сдвинула

| Что | В п. 1 дельты (19:36:50Z) | Действующее (19:46:01Z) |
| -- | -- | -- |
| знаков в описании | 80 334 | **81 032** |
| прирост от v1 (61 130) | 19 204 | **19 902** |
| `sha256[:16]` описания | `3c9fd3a00c2fd378` | **`8aa97d73d351c1f3`** |
| строк §7 | 18 | 18 — **не изменилось** |
| инвариантов | 24 | 24 — **не изменилось** |
| мест, меняющих ответы | 5 из 6 | 5 из 6 — **не изменилось** |

**Числа сняты прогоном по описанию, выгруженному отдельным запросом после записи правки.** Прежние числа п. 1 верны на свою минуту и не правятся: они там и остаются.

---

*Generated by [Claude Code](https://claude.ai/code)*