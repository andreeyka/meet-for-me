# ДЕЛЬТА `В` контракта C-018 — издание v4. Четыре места, пять правленых строк

**Первой строкой, как в `А` и `Б`:** действующий текст контракта живёт **в описании этой задачи** и там же издан в v4; дельта лежит комментарием по прямому указанию постановки [MEE-282](https://linear.app/easypto/issue/MEE-282), а не потому, что в описании кончилось место — в нём 100 515 знаков из 250 000.

**Зачем она существует.** Истории описаний Linear не хранит: правка v3 → v4 стёрла прежний текст безвозвратно. Эта дельта — единственное место, откуда редакция v3 восстановима посимвольно.

**Прежние разделы целы посимвольно:** дельты `А` и `Б` и оба их п. 10 не тронуты ни знаком.

## 0. Метка — `В`, взята собственным прогоном

**Область прогона названа вместе с тем, входит ли в неё сама запись.** Область — **все места серии C-018, десять текстов**: описание [MEE-276](https://linear.app/easypto/issue/MEE-276) (v4, выгружено отдельным запросом после записи издания — **100 515** знаков) и **все четыре** его комментария (дельта `А`, её п. 10, дельта `Б`, её п. 10 — выгружены одним `list_comments` после записи издания); описание [MEE-277](https://linear.app/easypto/issue/MEE-277) (**5 646**) и **все четыре** его комментария (**65 364 + 25 820 + 1 996 + 5 580 = 98 760**). План по C-018 не заведён. **Настоящий комментарий в область не входит:** он ложится после прогона.

**Счёт: занято две буквы — `А` и `Б`. Первая свободная — `В`.** Взятую метку я считал **не по упоминаниям, а по заголовкам разделов дельт** — «ДЕЛЬТА `X`», — потому что упоминание буквы и взятие буквы суть разное (правило 5): `А` взята комментарием 19:39:59Z, `Б` — 20:15:47Z. **Оба п. 10 буквы не берут и говорят это прямо**, а единственное совпадение шаблона на стороне [MEE-277](https://linear.app/easypto/issue/MEE-277) — фраза отчёта аналитика «Первая метка, когда понадобится, — `А`», то есть утверждение о **свободе**, а не о занятости. В описании v4 совпадений **ноль**.

**`В` законна:** не входит в четыре отведённые правилом (`Ъ`, `Ы`, `Ь`, `Ё`), пропущенных в этой серии нет вовсе — `А`, `Б` взяты подряд. Свободных односимвольных было 28, осталось 27.

**Постановка назвала `В` сама, и я это не принял на слово, а пересчитал** — по признаку §8 метку берёт исполнитель своим прогоном, и постановка прямо велела его повторить, потому что `Б` легла в 20:15.

**Длин четырёх комментариев [MEE-276](https://linear.app/easypto/issue/MEE-276) я здесь не печатаю** — способа, дающего побитовое совпадение, у меня для них нет, и это та же граница, что названа в отчёте по v3 и подтверждена приёмкой [MEE-281](https://linear.app/easypto/issue/MEE-281) §1. Числа без способа не печатаю.

## 1. Что изменилось — четыре места, пять правленых строк

| № | Место | Что правлено | Ответы |
| -- | -- | -- | -- |
| 1 | Заведение вне таблицы §7 | инвариант 2 (переписан), §7 преамбула (+абзац) | **меняются** |
| 2 | Окно между переходом и записью | инвариант 18 (переписан) | **меняются** |
| 3 | Предпосылка об идентичности встречи | инвариант 24 (дописан) | **не меняются** |
| 4 | `RecordingStatus` после `recover` | §10 перечень А, строка 1 (переписана), +абзац | **меняются** |

**Счёт по тексту v4, снятый прогоном после записи:** знаков — **100 515** (было 90 141, прирост **10 374**), `sha256[:16]` = **`b9ffb254b51e53b3`**; строк §7 — **18**, номера не тронуты ни один; инвариантов — **24**, номера не тронуты ни один, новых не заведено.

**Полнота этой дельты проверена не моим перечнем, а построчным диффом v3 против v4** — способом, который нашёл бы и неназванную правку. **Снято/заменено ровно пять строк**, и все пять названы ниже: `Версия`, §10 перечень А строка 1, инварианты 2, 18 и 24. **Всё прочее — чистое прибавление.** Неназванных правок нет.

---

## 2. Место 1 — заведение сессии вне таблицы §7 (инвариант 2; §7, преамбула)

### 2.1 Инвариант 2

**Было (v3), дословно:**

> 2. **Переходы — только строки таблицы §7, в её порядке; первое подошедшее условие побеждает.** Всякий иной переход запрещён. Отображение «состояние × вход → состояние» тотально: вход, не названный ни одной строкой для текущего состояния, состояния не меняет и ошибки не даёт.

**Стало (v4), действующий текст целиком:**

> 2. **Переходы — только строки таблицы §7, в её порядке; первое подошедшее условие побеждает.** Всякий иной переход запрещён. Отображение «состояние × вход → состояние» тотально: вход, не названный ни одной строкой для текущего состояния, состояния не меняет и ошибки не даёт. **Исключение ровно одно, оно названо поимённо и счётно: восстановительное заведение сессии в** `start(now:)` **по §10, перечню А — вход в** `processing` **и вход в** `failed`**, два способа и ни одного третьего.** Живых переходов это исключение не касается: всё, что происходит с уже заведённой сессией, — строки таблицы и только они. **Проверяемая форма:** перебор подаёт `start(now:)` на всяком содержимом хранилища и требует, чтобы сессии вне таблицы заводились ровно этими двумя способами, а всякий третий отсутствовал.

### 2.2 §7, преамбула — прибавлен абзац, прежний текст не тронут

**Было (v3):** преамбула состояла из одного абзаца — «Условия читаются в момент `tick(now:)` или в момент прихода названного входа. Строки проверяются **в порядке таблицы**: первое подошедшее условие побеждает.» **Он не тронут ни знаком.**

**Стало (v4), прибавленный абзац целиком:**

> **Восстановительное заведение сессии (§10, перечень А) этой таблицей не описывается, и это сказано здесь, а не выведено читателем.** Оно происходит **только** в `start(now:)`, ровно один раз за запуск приложения, и ни одним `tick` не повторяется; счёт его способов закрыт — их **два**, оба в §10 перечне А (вход в `processing` и вход в `failed`), и третьего нет. **Именно поэтому оно не внесено строками сюда:** строка таблицы читается в каждый `tick`, и заведения §10, став строками, звали бы `recover(directory:)` на одной и той же записи при каждом `tick`. Разбор обоих способов и цена каждого — §«Ломающие изменения против v3», п. 1.

**Таблица §7 не тронута ни знаком, и это существенно:** восемнадцать построчных критериев К29—К44 своих адресов снова не потеряли — третий раз подряд.

---

## 3. Место 2 — окно между переходом и записью статуса (инвариант 18)

**Было (v3), дословно:**

> 18. **Всякая смена состояния сессии с** `meetingId != nil` **записывается** `MeetingRepository.setStatus(_:meetingId:)` **(C-010) прежде, чем машина опубликует снимок в** `changes()`**.** Порядок значим: иначе потребитель, прочитавший хранилище по событию, прочтёт прежнее значение.

**Стало (v4), действующий текст целиком:**

> 18. **Всякая смена состояния сессии с** `meetingId != nil` **записывается** `MeetingRepository.setStatus(_:meetingId:)` **(C-010) прежде, чем машина опубликует снимок в** `changes()`**.** Порядок значим: иначе потребитель, прочитавший хранилище по событию, прочтёт прежнее значение. **Команда §3.1, уводящая сессию в терминальное состояние (**`skip`**, ответ** `.skip` **через** `answer`**), возвращает управление не раньше, чем её исход записан этим вызовом.** Отсюда граница окна названа точно, а не умолчана: падение до возврата команды теряет решение — и человек не получил подтверждения; падение после возврата решения не теряет. **Что даёт возврат** `setStatus` **для долговечности записи — предмет C-010** («Поведение»: один вызов репозитория — одна транзакция), и своего определения этот контракт не заводит.

**Почему закрыта именно половина окна, и почему вторая половина закрытия не требует** — разобрано в §«Ломающие изменения против v3», п. 2: из трёх причин терминального `skipped` две безвредны при потере записи (истёкший grace закрывает окно заведения сам; отмена события держится клаузой `isCancelled == false`), и вредна ровно одна — решение человека. Её и закрывает порядок возврата команды.

---

## 4. Место 3 — предпосылка об идентичности встречи (инвариант 24)

**Было (v3):** инвариант 24 кончался словами «…4 — про «сейчас», 24 — про «потом».» **Весь этот текст не тронут ни знаком** — прибавлено продолжение.

**Стало (v4), прибавленный текст целиком:**

> **Предпосылка этого пункта названа прямо, потому что без неё он верен не везде: идентичность встречи покрывает одно вхождение серии, а не серию целиком.** Адрес предпосылки — **C-001** («Что вне контракта»: правил повторения в `MeetingEvent` нет, плагин отдаёт уже развёрнутые вхождения) и **C-005** («Что вне контракта»: сторонам запрещено предполагать, что `DedupKey` совпадает у двух вхождений одной серии; разведение вхождений — обязанность плагина через уникальный `externalId`). **Цена отказа предпосылки названа здесь же:** если два вхождения серии всё-таки получат один `DedupKey`, C-005 сольёт их «в одну встречу с одним `id`», и этот пункт закроет серию целиком — первым же вхождением, ушедшим в `ready`, `failed` или `skipped`. **Чинится это не здесь:** ключ дедупликации — предмет C-005, и нужда в `interface-request` к нему открыта строкой отчёта по изданию v4.

**Ответов машины это не меняет ни одного:** меняется то, что читатель больше не выводит предпосылку сам. **Адрес найден чтением трёх чужих контрактов целиком** — C-001 v12, C-005 v3, C-010 v6, — и ни один из них не правлен ни знаком.

---

## 5. Место 4 — `RecordingStatus` после удавшегося `recover` (§10, перечень А)

### 5.1 Строка `.recording`, `.stopping`

**Было (v3), дословно:**

> | `.recording`, `.stopping` | `recover(directory:)` (C-004). Удалось — сессия входит в `processing` с восстановленным манифестом **и машина ставит первую недошедшую задачу цепочки (§8.7)**; бросил `recoveryFailed` — сессия входит в `failed`. `origin` берётся из `manifest.meetingId`: `nil` — `.adHoc` |

**Стало (v4), действующий текст целиком:**

> | `.recording`, `.stopping` | `recover(directory:)` (C-004). **Удалось** — машина **сперва приводит** `RecordingStatus` **этой записи к** `.finalized` (`RecordingRepository.save(_:)`, C-010, с восстановленным манифестом), **и только затем** сессия входит в `processing` **и ставится первая недошедшая задача цепочки (§8.7)**. **Бросил** `recoveryFailed` — машина приводит `RecordingStatus` к `.failed`, и сессия входит в `failed`. `origin` берётся из `manifest.meetingId`: `nil` — `.adHoc` |

**Строки `.finalized` и `.failed` перечня А не тронуты ни знаком**, и перечень Б не тронут ни знаком.

### 5.2 Прибавленный абзац перед перечнем Б

**Стало (v4), действующий текст целиком:**

> `RecordingStatus` **приводится к фактическому состоянию машиной, и это издание v4.** До него §10 звал `recover`, вводил сессию в `processing` либо `failed` — и оставлял строку `recordings` в `.recording`: **та же запись возвращалась из** `unfinalized()` **(C-010) при каждом следующем запуске, и** `recover` **звался на ней снова.** Дубликата задачи это не давало (§8.7 не ставит задачу, у которой уже есть ждущая или идущая), но запись не финализировалась никогда. **Обязанность названа не здесь, а у соседа, и здесь она только исполняется:** C-010 («Поведение») говорит, что строка `recordings` со статусом `recording` при старте означает прерванную запись, что обработка — «дочитать манифест с диска, финализировать или пометить `failed`» — **обязанность домена, а не репозитория**, и что `unfinalized()` существует именно для этого. **Порядок значим по тому же доводу, что в инварианте 18:** сперва запись, потом вход в `processing`, иначе падение между ними оставляет запись нефинализированной, а цепочку — поставленной.

---

## 6. «Что вне контракта» — четыре новые строки, прежние восемнадцать не тронуты

**Стало (v4), действующий текст целиком:**

> * **§10 заводит сессии теми же строками, что таблица §7** — не теми: восстановительное заведение таблицей не описывается вовсе, происходит только в `start(now:)` и насчитывает ровно два способа (инвариант 2, §10 перечень А);
> * **решение человека, отданное командой, переживает падение приложения в любой момент** — не в любой: окно между переходом и записью `setStatus` названо границей, и закрыто оно только для команд, которые не возвращают управления до записи (инвариант 18);
> * **машина сама решает, сливаются ли два вхождения повторяющейся серии в одну встречу** — не решает: идентичность встречи задают C-001 и C-005, и этот контракт на неё **опирается**, называя предпосылку и адрес (инвариант 24);
> * `recover` **оставляет** `RecordingStatus` **как был** — не оставляет: машина приводит его к `.finalized` либо `.failed` прежде входа в `processing` (§10, перечень А);

---

## 7. Что ещё прибавлено и ничего не заменяет

1. **Шапка:** строка «Основание версии 4». Строки «Основание версии 1», «Основание версии 2» и «Основание версии 3» **не тронуты** — датированные места (§7).
2. **«Запись о выполненной проверке издания v4»** — новое датированное место рядом с тремя прежними; ни одно из них не тронуто ни знаком. **Она называет три прочитанных чужих контракта с их изданиями и объёмами** — впервые за серию v2—v4.
3. **Раздел «Ломающие изменения против v3»** — новый, стоит **перед** разделами против v2 и против v1 и **рядом** с ними. Все три помечены «сохранено», дельта критериев не процитировала ни одного.
4. **Заголовок задачи** приведён с «C-018 v3» к «C-018 v4».

## 8. Чего эта дельта не трогает

**Таблица §7 — ни знаком.** §8.2, §8.7, §9.1, §9.2, §9.3, §10 перечень Б, инварианты 4, 17, 19, 20, 22 — ни знаком. Разделы «Ломающие изменения» против v1 и против v2 — ни знаком.

**Перечень [MEE-277](https://linear.app/easypto/issue/MEE-277) — ни символом.**

**Чужие контракты — читаны и не правлены:** C-001 v12 ([MEE-2](https://linear.app/easypto/issue/MEE-2)), C-005 v3 ([MEE-9](https://linear.app/easypto/issue/MEE-9)), C-010 v6 ([MEE-18](https://linear.app/easypto/issue/MEE-18)) — **ни знаком ни в одном**. `interface-request` **не заведён**: нужда в нём открыта строкой отчёта, номер присваивает РП (§9 правил).

**Кода, `docs/` и `ci.yml` не тронуто ни строкой; задач не заведено, номеров IR не присвоено.**

---

*Generated by [Claude Code](https://claude.ai/code)*