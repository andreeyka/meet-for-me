## Дельта `Ц`, п. 6 — **седьмое место издания** и числа, которые оно сдвинуло. Внесено **до** отчёта, своим прогоном. Буквы не берёт

**Метки этот пункт не берёт** — он часть дельты `Ц`.

### 1. Почему седьмое место внесено после дельты, а не было в ней

**Перечисляя лежащие строки своего предмета прежде новых** (правило, которое приёмка [MEE-299](<https://linear.app/easypto/issue/MEE-299>) §3 завела по моей же находке), я нашёл **четвёртую**, а не три: половину архитектора у К71 — «§9.1 не разделяет, отдаёт ли `plan(now:)` строку встречи с терминальным `MeetingStatus`» (дельта `Х` перечня [MEE-277](<https://linear.app/easypto/issue/MEE-277>), §7.5; отчёт [MEE-301](<https://linear.app/easypto/issue/MEE-301>) §7). **Её срок — «не позже выдачи задачи на §10» — тот же самый, что у находки 7, и наступил он в ту же минуту.** Постановка [MEE-302](<https://linear.app/easypto/issue/MEE-302>) её не называет: её предмет — три места, и это четвёртое.

**Не внести её значило бы просрочить её в минуту выдачи части C** — ровно механизм, который приёмка MEE-299 §3 и разбирала на находках 6, 7 и 8, и ровно довод, которым аналитик внёс К45 сверх объёма (дельта `Х`, Решение 4; принято приёмкой [MEE-301](<https://linear.app/easypto/issue/MEE-301>) Р-IV).

**Законность по стоп-правилу [MEE-281](<https://linear.app/easypto/issue/MEE-281>) §8 проверена, а не предположена, и она не от противоречия:** место **не меняет ни одного ответа ни на одном входе**, а правка, не меняющая ответов, издания не требует вовсе (§8 правил проекта) — то есть круга не тратит и стоп-правилу не противоречит. Это тот же довод, которым издание v5 внесло свои места 2—5.

### 2. Участок 16 — ЗАМЕНА. v5: строка 523. v6: строка 580 (§9.1, последний пункт)

**БЫЛО** — действующий текст v5, целиком:

> * `plan(now:)` возвращает `ScheduledArm` для всех таких событий по возрастанию `armAt`, при равенстве — по `meetingId`; сессий это не заводит и состояния не меняет — **чистая функция** от содержимого хранилища, настроек и `now`.

**СТАЛО** — текст v6, целиком:

> * `plan(now:)` возвращает `ScheduledArm` для всех таких событий по возрастанию `armAt`, при равенстве — по `meetingId`; сессий это не заводит и состояния не меняет — **чистая функция** от содержимого хранилища, настроек и `now`. **«Таких» разобрано по клаузам, а не оставлено читателю, и это издание v6:** событие входит в ответ, если истинны три первые клаузы условия заведения — `isAllDay == false`, `isCancelled == false`, `now ≤ graceEndsAt` — **и** хранимый `MeetingStatus` встречи не терминален; клауза «у встречи нет живой сессии» к `plan(now:)` **не применяется** — встреча с живой сессией в плане стоит. **Ответа это не меняет ни на одном входе: обе половины уже были вынуждены инвариантом 17, и сказано здесь то, что он уже требовал, а §9.1 не говорил.** Довод читается на его трёх ветвях: ветвь «заведённая раньше и живая» была бы **недостижима**, исключай план встречи с живой сессией; а строка плана у встречи с хранимым терминальным статусом **нарушала бы инвариант 17 целиком** после перезапуска приложения: сессии ей не даёт инвариант 24, ветвь «терминальная, если она уже ушла … **строками таблицы**» говорит о сессии этого запуска, а её нет ни одной. **Цена названа:** `nextDeadline(now:)` держит `armAt` встречи с живой сессией (инвариант 15), и composition root зовёт `tick`, который по этой встрече ничего не меняет. **Отвергнуто — сузить план до встреч без живой сессии:** это убило бы ветвь инварианта 17 молча, а цена мёртвой клаузы этим контрактом уже названа дважды — верхней границей строки 2 §7 и клаузой строки 9.

### 3. Две правки в абзаце «Ломающие изменения против v5» — мои, в собственном датированном месте, до отчёта

**Первая — счёт мест.** БЫЛО: «**Правлено шесть мест. Ответы меняются в двух; остальные четыре** не меняют ни одного ответа ни на одном входе…». СТАЛО: «**Правлено семь мест. Ответы меняются в двух; остальные пять** не меняют…».

**Вторая — пункт 6 абзаца заведён под §9.1, а прежний пункт 6 («Шапка») перенумерован в 7.** Пунктов в абзаце стало семь, и нумерация 1—7 сплошная, без пропусков и без двух шестёрок — **проверено прогоном по самому тексту, а не глазом:** `re.findall` по заголовкам пунктов внутри абзаца даёт ровно `['1','2','3','4','5','6','7']`. **Столкновение двух «6» простояло один ход и было снято до отчёта** — оно и есть причина, по которой этот пункт печатает признак проверки, а не только исход.

**Датированное место правлено мной, в минуту его написания, и §7 правил этим не нарушен:** запрет §7 защищает текст, верный на **свою** дату, от правки **задним числом**; абзац датирован сегодняшним изданием, дельты критериев по нему нет, и его дата — эта же минута.

### 4. Числа дельты, ставшие недействующими, и их действующие значения

**Это второй сдвиг тех же чисел за ход** (первый — п. 5 выше), и я печатаю оба, а не только последний, чтобы приёмка видела ряд, а не итог.

| Величина | В дельте | п. 5 | **Действующее (п. 6)** | Способ |
| -- | -- | -- | -- | -- |
| C-018 v6, знаков | 139 225 | 139 221 | **141 830** | `get_issue` **отдельным запросом после записи**, ответ лёг файлом, `len` над `description` |
| C-018 v6, `sha256[:16]` | `a5e4ade9b99a9191` | `6259e05654710b79` | **`e629370f813850b5`** | там же, `hashlib` над тем же полем |
| Прирост от v5 | 24 225 | 24 221 | **26 830** | 141 830 − 115 000 |
| Участков | 18 | 18 | **19** | `difflib.SequenceMatcher`, `autojunk=False` |
| Строк снято либо заменено | 17 | 17 | **18** | там же |
| Строк прибавлено либо заменено | 80 | 80 | **83** | там же |
| Мест издания | 6 | 6 | **7** | перечислением в §1 дельты и в абзаце ломающих изменений |
| Участок 3 (запись о проверке + абзац ломающих), знаков / `sha256[:16]` | 9 572 / `bf6744705037f5a7` | то же | **10 867 / `66b753889cc53a87`** | блок вырос пунктом 6 абзаца и правкой счёта мест |
| Участок 4 (пометка «сохранено»), знаков / `sha256[:16]` | не печатался | — | **908 / `56140ea60863a593`** | печатается здесь, потому что п. 5 его правил |

**Что НЕ изменилось и проверено повторным прогоном:** участок 2 — **2 434** / `8db62c4f19c5c7a8`; участок 17 (бывший 16, §10) — **2 465** / `e6980ede6f84957f`, сдвинулся только номером и адресом (v6 строки 619—626); сумма v5 — **115 000** / `b2643187d321aefa`; строк таблицы §7 — **19**; правил §5.4 — **пять**; инвариантов — **24**; артефактов `\*\*` в тексте — **ноль**, строк с нечётным числом `**` — **ноль**.

**Нумерация участков после вставки: участки 1—15 сохранили номера, прежние 16, 17, 18 стали 17, 18, 19**, а номер 16 занял §9.1. **Ссылки дельты выше на «участок 16 (§10)» читать как «участок 17»** — это единственная ссылка, которую сдвиг задел, и я называю её поимённо, а не оставляю читателю.

### 5. Что меняется у смежных ролей сверх уже названного

**У аналитика ([MEE-277](<https://linear.app/easypto/issue/MEE-277>)) — прибавляются два номера к строке, названной в отчёте:** **К71** и **К74**. Дельта `Х` сузила Вход К71 клаузой «у всех подаваемых событий хранимый `MeetingStatus` нетерминален» **именно затем, чтобы не решать за контракт** (её §7.5, форма взята у К90). **Контракт ответил, и снятый вход возвращается — отрицательным вектором:** встреча с терминальным хранимым статусом в плане **не стоит**, встреча с живой сессией — **стоит**. Оба входа сегодня не подаёт ни один вектор дерева.

**У QA ([MEE-288](<https://linear.app/easypto/issue/MEE-288>)) — те же два входа в дельту плана**, после дельты критериев.

**У DEV (часть C) — предмет не растёт ни на строку, а сужается:** `plan(now:)` перестаёт быть местом, где реализатор выбирает чтение сам.

---

*Generated by [Claude Code](<https://claude.ai/code>)*