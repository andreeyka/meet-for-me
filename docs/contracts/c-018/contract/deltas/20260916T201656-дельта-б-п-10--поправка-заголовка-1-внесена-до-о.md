## Дельта `Б`, п. 10 — поправка заголовка §1. Внесена **до** отчёта, своим прогоном. Буквы не берёт

**Метки этот пункт не берёт** — подразделы внутри дельты нумеруются, а не обозначаются буквами (§8). В серии C-018 занято две буквы: `А` и `Б`; первая свободная — `В`.

### Что было неверно, и это опровергалось таблицей под самим заголовком

**Было (v3, 20:15:47Z):**

> ## 1. Что изменилось — одно место в шести точках, и все шесть меняют ответы

**А таблица прямо под этим заголовком помечает точки 5 и 6 как «не меняются».** Заголовок и его собственная таблица давали два ответа на один вопрос — **ровно тот класс, который это издание и чинит**, и найден он через полторы минуты после записи, моим прогоном по своему же тексту.

**Стало, действующий текст целиком:**

> ## 1. Что изменилось — одно место в шести точках; правило меняется в двух из них

### Как считать правильно — и почему счёт «шесть» был снят не тем способом

**Правило в этом контракте печатается дважды: прозой и инвариантом.** Здесь оно напечатано в §9.1, первый пункт, и в инварианте 24 — **обе точки правлены, и ответы машины меняются один раз, а не дважды**: это одно правило в двух изложениях, как того требует форма контракта.

**Остальные четыре точки приведены с ним в согласие и собственных ответов не меняют:**

| Точка | Чем она является | Ответы |
| -- | -- | -- |
| §9.1, первый пункт | **правило** (проза) | **меняет** |
| Инвариант 24 | **правило** (проверяемая форма) | **меняет** |
| Инвариант 4, вторая половина | ссылка на инвариант 24 | не меняет |
| §9.1, абзац «Вторая сессия…» | довод, цена и два отвергнутых | не меняет |
| §10 перечень Б, строка | следствие §9.1 | не меняет |
| «Что вне контракта», две строки | отрицания, приведённые к новому тексту | не меняет |

**Счёт «шесть» был снят чтением структуры — по числу правленых точек, — а назван как число, снятое перечислением ответов.** Это тот же класс, что «политики §8 — восемь подразделов» в постановке MEE-277 и находка `АБ.11`: два разных счёта одного места, и меньший из них выдан за больший.

**Цена, если бы осталось.** Аналитик считает предмет дельты критериев по этой таблице. По заголовку «все шесть меняют ответы» он завёл бы **шесть** предметов вместо **двух**, и четыре из них были бы критериями на прозу — на абзац довода, на ссылку внутри инварианта 4, на строку-следствие и на отрицания «Что вне контракта». **Критерий на прозу зелен по построению** (правило 7), а таблица покрытия перечня перестала бы отвечать тексту контракта. **Издания эта поправка не требует:** правится утверждение **о** правке, а не правило; полное «было → стало» напечатано здесь, в проверяемом месте (§8).

### Числа дельты, которые эта поправка не сдвинула

Знаков в описании — **90 141**, `sha256[:16]` = **`10db24e9eb013c0f`**, строк §7 — **18**, инвариантов — **24**, правленых точек — **6**, область прогона метки — **218 089 знаков в восьми текстах**. Ни одно из них поправкой не затронуто: сдвинулся счёт **ответов**, а не счёт точек.

**И одно число дельты, проверенное отдельно после записи:** «прочие шестнадцать строк раздела „Что вне контракта“ не тронуты» — прогоном по обоим текстам: в v2 их **17**, в v3 — **18**, одна переписана, одна заведена, **16** не тронуты. Сошлось.

---

*Generated by [Claude Code](https://claude.ai/code)*