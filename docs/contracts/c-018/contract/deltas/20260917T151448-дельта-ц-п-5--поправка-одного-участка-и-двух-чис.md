## Дельта `Ц`, п. 5 — поправка одного участка и двух чисел. Внесена **до** отчёта, своим прогоном. Буквы не берёт

**Метки этот пункт не берёт** — он часть дельты `Ц`, а не новая дельта (§8 правил: подразделы внутри дельты нумеруются, буквы дороже).

### 1. Что было неверно — моя опечатка в тексте контракта, а не в дельте

В участке 4 (пометка «сохранено» на абзаце «Ломающие изменения против v4») я оставил **непарные** `**` в конце предложения. Linear при записи нормализовал их в **литеральные** `\*\*`, и в действующем тексте контракта они видны читателю как два символа звёздочки посреди фразы.

**БЫЛО** — действующий текст v6 на минуту записи издания, конец абзаца, целиком:

> Сверх того у абзаца нет номера запроса (сказано ниже), а абзац без номера запроса не роняется никогда.\*\* Он стоит **рядом** с абзацами против v3, против v2 и против v1;

**СТАЛО** — действующий текст v6, целиком:

> Сверх того у абзаца нет номера запроса (сказано ниже), а абзац без номера запроса не роняется никогда. Он стоит **рядом** с абзацами против v3, против v2 и против v1;

**Снято четыре знака: `\*\*` и один пробел перед «Он».** Ни одного слова не тронуто. **Ответов машины не меняется ни одного**, утверждений — ни одного; переизданием это не является (§8 правил: правка, не меняющая ни одного ответа ни на одном входе, издания не требует при напечатанном «было → стало» в проверяемом месте).

**Цитата «СТАЛО» участка 4 дельты выше несёт ту же опечатку** — она снята настоящим пунктом и в дельте, и в контракте; действующей считается редакция этого пункта.

### 2. Два числа дельты, ставшие недействующими, и их действующие значения

| Величина | Напечатано в дельте | **Действующее** | Способ |
| -- | -- | -- | -- |
| C-018 v6, знаков | 139 225 | **139 221** | `get_issue` **отдельным запросом после поправки**, ответ лёг файлом, `len` над полем `description` |
| C-018 v6, `sha256[:16]` | `a5e4ade9b99a9191` | **`6259e05654710b79`** | там же, `hashlib` над тем же полем (второй способ §11) |
| Прирост от v5 | 24 225 | **24 221** | 139 221 − 115 000 |

**Что НЕ изменилось и проверено повторным прогоном, а не оставлено по умолчанию:** участков по-прежнему **18**, строк снято либо заменено — **17**, прибавлено либо заменено — **80**; длины и суммы трёх чистых прибавлений целы до знака — участок 2 **2 434** / `8db62c4f19c5c7a8`, участок 3 **9 572** / `bf6744705037f5a7`, участок 16 **2 465** / `e6980ede6f84957f`; строк таблицы §7 — **19**, правил §5.4 — **пять**, инвариантов — **24**. Сумма v5 (**115 000** / `b2643187d321aefa`) поправкой не затрагивается вовсе.

### 3. Класс ошибки называю, потому что он не про невнимательность

**Опечатку нашёл не я глазами, а прогон, которого прежние издания этого контракта не ставили:** счёт `**` по каждой правленой строке на чётность плюс поиск литерала `\*\*` по всему тексту. **Оба дали ноль по чётности и один по литералу** — то есть первый признак опечатку **не видит**, потому что нормализация Linear делает число `**` чётным, съев непарную пару в литерал. **Признак, который её ловит, ровно один: поиск `\*\*` в выгруженном тексте.**

**Почему это стоит отдельного пункта, а не молчаливой правки.** Дельта `Ф` уже наблюдала ту же нормализацию с другой стороны: там Linear **дописывал** `**`, вынося инлайн-код из полужирного, и разница в 12 знаков была названа причиной, а не подогнана. Здесь тот же механизм сработал наоборот — **съел** непарную пару, — и разница в 4 знака нашлась бы приёмкой как расхождение сумм, если бы я её не назвал. **То есть у нормализации Linear два направления, и в дельте `Ф` названо было одно.** Признак для следующего издания: после записи искать в выгруженном тексте литерал `\*\*`, а не считать `**` на чётность.

**Почему поправкой, а не правкой дельты на месте:** правка сделала бы запись верной и скрыла бы, что в ней было неверно.

---

*Generated by [Claude Code](<https://claude.ai/code>)*