# Plugins/graph — модуль `plugin-graph`, владелец DEV-2

Внешний процесс-коннектор Outlook через Microsoft Graph: JSON-RPC по stdio, MSAL, delta-синхронизация.
Секреты — только через сервис `secrets` хоста, на диск плагин ничего не пишет.

Вне Среза 1.
