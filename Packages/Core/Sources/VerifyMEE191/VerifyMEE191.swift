//  ВРЕМЕННЫЙ таргет проверочной ветки MEE-191 (возврат РП 24.09, п.2). Не продукт, не сдаётся,
//  в main не попадает — снимается вместе с прочими нарушителями этой ветки.
//
//  Имитирует стороннюю зависимость наподобие GRDB: свой тип `Date`, который совпадает ИМЕНЕМ
//  с разрешённым `Foundation.Date` из `allowed-types/Storage.json`, но им не является. До этого
//  возврата сверка символьного графа сравнивала только имя (`spelling in allowed_entry["types"]`)
//  и не отличила бы это `Date` от настоящего `Foundation.Date` — ровно та находка РП, «GRDB.Data
//  проходит как Foundation.Data». Пара «модуль, имя» обязана здесь покраснеть.
//
//  Отдельный таргет, а не объявление прямо в `DomainCore`/`Storage`: у `Date` в их собственных
//  файлах десятки словоупотреблений (Foundation), и то же имя в их собственном пространстве имён
//  затенило бы настоящий `Foundation.Date` и сломало бы сборку модуля, а не проверило бы разбор
//  графа.

public struct Date: Sendable {
    public init() {}
}
