//  CalendarHub
//
//  Модуль: calendar-hub · Владелец: DEV-1 [v1.11, MEE-353 — передан от DEV-2] · Слой: домен
//
//  Каталог принадлежит владельцу модуля: файлы здесь изменяет только он (П1).
//  Всё, что пересекает границу модуля, описано контрактом архитектора и меняется
//  только через interface-request (П2, П6). Границы и запреты — docs/module-map.md.
//
//  Реализует CalendarPort (C-005 v8) и хост-роль CalendarConnector (C-006 v12) для
//  in-process коннекторов. Перечень приёмки — MEE-347, план проверки — MEE-361, задача на
//  реализацию — MEE-362.
//
//  Единственный публичный тип этого модуля, реализующий CalendarPort, — CalendarPortImpl
//  (CalendarPortImpl.swift). Seams.swift объявляет три шва (секреты, stdio-транспорт,
//  ожидание) — перечень называет их операции, форму решает этот модуль (закрыто C-006 v9,
//  IR-118: тестовые реализации живут в CalendarHubTests, не в DomainTestKit).
//
//  Не входит в эту правку (следующие части MEE-362): stdio-адаптер (группа Ж перечня —
//  кадрирование, коды §5.1, курсор — CalendarConnector поверх Ш2); честное слияние
//  скаляров/attendees по правилу п.2-3 и ассоциативность (К20-К24, К28). Шесть новых
//  методов CalendarPort (группы К/Л, MEE-355) слились в main раньше срока — РЕАЛИЗОВАНЫ
//  этой правкой (CalendarPortImpl.swift), но без своих тестов: покрытие (К3 вход Б,
//  К66-К69, К71-К75) — следующая часть, файлы ControlSurfaceEntryPointsTests.swift/
//  SourceRoutingTests.swift по плану MEE-361.
