//  AttributionSource — контракт C-015 (MEE-23), §1 «Источник и уверенность назначения»
//
//  Модуль: domain-core · Владелец: DEV-2 · Слой: домен
//
//  Только объявления (MEE-289). Реализацию порта атрибуции пишет модуль `attribution`
//  (Packages/Core/Sources/Attribution/).
//
//  ПОЧЕМУ ЭТОТ ТИП ЗДЕСЬ, ХОТЯ ПОСТАНОВКА MEE-289 НАЗВАЛА ПЯТЬ КОНТРАКТОВ, А НЕ ШЕСТЬ.
//  `TranscriptRepository` (C-010 §5) — один из шести портов предмета, и две его подписи
//  несут `SegmentRow` и `SegmentAttributionUpdate`, у которых поле объявлено типом
//  `AttributionSource`. Объявить порт, не объявив этот тип, нечем: он не компилируется.
//  Разрешённый список инварианта 19 C-010 называет `AttributionSource` прямо, с пометкой
//  «(C-015)». Найдено прогоном, а не взято из перечисления §6 плана, где этого имени нет.
//  Назначения смысла типу здесь не делается ни одного: состав и комментарии — дословно §1 C-015.

import Foundation

public enum AttributionSource: String, Codable, Sendable, CaseIterable {
    case micChannel       // сегмент пришёл с канала mic (C-003): о канале, а не о говорящем
    case voiceProfile     // совпадение эмбеддинга кластера с голосовым профилем участника
    case oneOnOne         // встреча один на один: единственный системный кластер = второй участник
    case textualHint      // самопредставление или обращение по имени (вне Среза 1)
    case nameDictionary   // текст сегмента правился по словарю имён участников
    case user             // человек назвал кластер вручную
}
