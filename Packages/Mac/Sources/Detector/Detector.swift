//  Detector
//
//  Модуль: detector · Владелец: DEV-1 · Слой: адаптер системного API
//
//  Каталог принадлежит владельцу модуля: файлы здесь изменяет только он (П1).
//  Всё, что пересекает границу модуля, описано контрактом архитектора и меняется
//  только через interface-request (П2, П6). Границы и запреты — docs/module-map.md.
//
//  Реализует C-009 (MEE-15): `ProcessMonitorPort` и `PlatformResolver` одним публичным типом
//  `MeetingDetector`. Устройство модуля — по швам перечня MEE-75:
//
//  * Ш1 — сборка снимка чистой функцией: `SnapshotBuilder`;
//  * Ш2 — чтение ресурса одним местом (`DetectorResources`) и построение правил из байтов
//    чистой функцией (`RuleTables.build`);
//  * Ш3 — отображение кода возврата системного вызова в ошибку порта: `HALStatusMapping`;
//  * Ш4 — источник снимков и точка подачи наблюдения: `ProcessSnapshotSource`, отдающий пару
//    «содержание + момент» (`TakenSnapshot`, свойства (i) и (ii)), и `ObservationDriver`
//    (свойство (iii));
//  * Ш5 — точка приёма значений таблицы весов: публичный инициализатор `MeetingDetector`;
//  * Ш6 — часы, по которым решается срок подтверждения: `ObservationClock`, `SignalEngine`.
